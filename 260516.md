## oop - python
### 파이썬 (python)
- 파이썬 공식 레퍼런스 문서 == 파이썬계의 헌법이자 바이블 (사유: 파이썬 제작자가 작성함)
- 파이썬 언어 구현은 파이썬 레퍼런스를 준수하며 만들어짐
- 공식 문서를 보며 이해할 수 있는 직관력을 기를것
- [공식문서](https://docs.python.org/3/reference/index.html)

### OOP(객체 지향 프로그래밍, object-oriented programming)
- java는 클래스가 단위, 파이썬은 모듈이 단위
- 프레임워크
    - 자바는 스프링과 같은 예시를 들 수 있음
    - 프레임워크 클래스를 제공함
    - 틀과 클래스 공부를 열심히 하자
- 클래스
  - 메서드: 클래스에 속한 함수
    - 메서드에는 매개변수에 self 가 들어가지만 함수는 들어가지 않음
  - 데이터와 기능을 묶는 기능 제공
- 데이터 클래스 (오늘의 키워드 선정)
  - 반복적으로 사용되는 코드를 자동으로 생성함으로써, 오로지 데이터 저장 및 관리를 목적으로 사용하는 클래스를 선언할때 사용하는 기능
  - @ == 데코레이터 (클래스 앞에 붙이면 클래스 데코레이터/ 선언시 필수는 아님)
    - 기존에 작성된 코드를 수정하지 않고도 앞뒤에 원하는 추가 기능을 쉽게 덧붙일 수 있게 해준다 
    - 기능은 주로 로깅, 권한 확인 등이 있음
  - 클래스 앞에 @Dataclass 라고 쓰는 방식으로 클래스에 수식
  - AI는 통계가 필요하고 통계는 데이터가 필요함
    - 즉 초기화, 문자열 표현, 상등 비교등을 위해 데이터 클래스 사용
    - 속성 관리라도 합시다
## Concurrency (동시성)
- 오늘의 키워드 선정
- 코드가 실행할때 어떻게 돌아가는지 보는 실행적 관점이 필요
  - 코드 작성 : 명령 하달서
  - 코드 작성자 : 시나리오 작가
  - 파이썬 실행기(ex. 런타임/인터프리터) : 감독
- os 익히세요 프로세스-스레드 하려면 배우셔야 합니다

### Thread
- 하나의 프로스세 안에서 실행되는 >> 작은 실행 단위<< 로 , 메모리를 공유하여 여러 작업을 동시에 처리하는데 사용
- GIL (Global Interpreter Lock)
  - 개발자면 알고 있자 모르면 개발자 아님
  - 한 시점에 하나의 스레드만 파이썬 바이트 코드를 실행하도록 제한하는 락 (예전에 파이썬내 레퍼런스 카운팅을 멀티스레드가 동시에 건들이면 터지는 일이 있어 걍 동시 접근을 막아버림)
    - 따라서 파이썬은 여러개의 스레드가 사용 불가함 (컨텍스트 스위치, cpu가 하던일을 빠르게 바꿔 동시에 하는것처럼 보이도록 하는 행위 사용 불가)
    - 멀티 스레드 완전 사용 불가는 아님, 사용은 가능
      - but cpu는 동시 작업이 불가하고 컨텍스트 스위치는 지원이 안되므로 작업의 빠른 전환이 불가하여 사용 의미가 없음, 멀티 스레드 사용시의 이점을 보지 못하여 작업을 대기해야함 
      - I/O 때문에 사용, 가령 네트워크 응답을 기다리는 등 대기 시간이 길어질때 다른 작업을 돌리는 용 (즉, 안정성이 보장될때만 사용)

### Process
- 1 코어 1 프로세스
- 하나의 코어에서 여러개의 프로세스를 돌릴때에는 동시에 작업할 수 없으니 프로세스간의 전환을 이용
  - Heavyweight context switching : 프로세스 간의 전환
  - Lightweight context switching : 스레드들 간의 전환
- 하지만!!! 멀티코어의 개발로 정말 동시 실행 및 작업이 가능 해짐!!
- AWS, GCP, Colab 같은 서버 코어는 가상 코어를 가지고 있음 (코어수가 적다)
- 멀티 프로세스 : 운영체제로부터 독립된 메모리 공간을 할당 받아 실행중인 하나의 프로그램 (메인이랑 완전히 따로노는 친구라는 뜻), 파이썬에서는 GIL의 한계를 넘어 cpu의 병렬을 처리하기 위할때 주로 사용
- 즉 스레드는 길의 영향을 받지만 프로세스는 받지 않음
- ex.영상 여러개를 동시에 인코딩할때 멀티 코어 개수만큼 비디오를 하나씩 할당받아 각 프로세스가 한개의 비디오를 인코딩해냄 

## Async (비동기)
- 단일 스레드
- 단일 스레드 내부에서 작업을 전환하는것 (프로세스나 스레드 자체를 스위치 하지는 못하는 대신 작업 순서는 스위치 한다)
- 스케줄링 (but cpu가 아닌 파이썬이 하는)
- 네트워킹(api 호출) 중이거나 api 호출이 여러번일때 주로 사용
- async await: 싱글 스레드 환경에서 특정 작업이 끝나기를 기다릴때 다른 작업을 처리하도록 비동기 프로그래밍이 동기식으로 돌아갈 수 있도록 작성해주는 키워드 
## 이번주 key word
- Git 기본 마스터
- Python 응용: 예외처리, 이터레이터, 제네레이터, 파일 I/O
## 팀 미팅
- 많은걸 배울때 기존에 알고 있는걸 버리자
- 새로운걸 배울때는 덜어내는걸 하자 (지금은 해당 사항 아님)
## 기타
### 바인딩
- 바인딩 : 둘을 연결하는 것, 그러한 역할군
- 즉 바인딩 개발은 웹을 앱으로 포장하는것을 의미
### 에디터
- 에디터: 편집기(텍스트, 이미지, 게임, 영상)
- 에디터 엔진: 코드의 핵심 구동 축과 편집기를 같이 부른것
- 가령 vscode 는 엔진이 js 로 만들어졌다
