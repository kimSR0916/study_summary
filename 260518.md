# 4주간 일정 정리
- 한줄 정리에 정의 뿐만 아니라 사용 이유도 포함시킬것
- 1:N 멘토링 미해당 팀은 걍 자습
- 딥다이브 이번주 처음해 꺅!! 딥다이브 주제를 금욜에 공개해드려요~♡
- 토요일 스크럼 잊지 말기

# 복습
- 파이썬 프로그래밍 언어
- 자유로운 구사
- 내가 뭘 모르는 지 모른다 (뭘 모르는지 안다는 되어야혀)
  - 모르는걸 알려면 우뜩해야할가
- 수학은 암기여 ;; (복습을 자주 하자)

# 네트워크 
- 클라우드는 네트워크가 중요
- 데이터를 주고 받기 위한것

## 노드
- 연결 된 애들, 당사자들 (ex. 서버, 클라이언트)
  - 서버 : 응답의 주체
  - 클라이언트 : 요청의 주체
- 데이터를 생성, 수신, 또는 정송하는 장치나 지점
- 네트워크에서 고유한 주소를 가지고 있음
- 모뎀은 아날로그/디지털 신호 변환 장치이므로 주고받는 데이터가 없어 노드로 간주되지 않음
- 각 장치가 노드의 역할을 하며 데이터를 보내거나 받는 역할을 하는 노드가 없으면 데이터의 송수신이 이루어지지 않음 (주고 받을 객체가 없음!!)

## TCP/IP
- 응욜, 전송, 인터넷, 네트워크 인터페이스 계층 지원
- 하위 걔층으로 가면서 각 단계마다 캡슐화를 하는것
- 컴퓨터 간 통신을 위한 표준 프로토콜 (OSI는 세분화 되어있고 7계층 전부를 사용하지만 TCP/IP는 전 계층을 지원함)
- 소켓: 네트웍을 프로그래밍 하기 위해 추상화 한것
- 인코딩/ 디코딩 메커니즘에서 시작점은 항상 인코딩(문자 -> 바이트(수) -> 문자 인코딩 바이트 (수) -> 문자 디코딩(인코딩 해제), 애초에 퍼즐이 있어야 문제를 풀것 아닌가)
- 클로즈 하지 않으면 네트워크 자원 회수가 매끄럽지 못하므로 close를 잊지 말자
- 포트 : 주소의 일부, 소켓 : 그 포트를 모아 추상화 (훨씬 더 큰 개념)
- 서버가 계속 요청을 받게 하는 방법
  - 무한 루프 : 무한 루프가 그렇게 나쁜게 아녀, 웹서비스가 24시간 내내 돌아가게 해준다
    - 서버 쪽에선 이를 event loop 이라고 부름
  - 서버 개발자 on-call 개발 방법 채택, 서버 다운되면 안되니까 당직을 선다
  - 즉 죽는 서버의 장애 등급은 서버 자체의 중요도에 따라 달라짐(메인 홈페이지는 1등금, 부가 서비스는 3등급)

# Http (Hyper Text Trandfer protocol)
- 하이퍼 텍스트 전달 프로토콜, 구조화 된 텍스트를 전송하기 위해 사용되는 통신 규약
- 글을 뛰어 넘는 글, 즉 멀티미디어  (ex. jpg, png, html, css, audio, link)
  - text: html, css, js
  - Binary: Image(jpg, png)
- 서버와 클라이언트가 하이퍼 텍스트 문서를 정의된 규칙에 따라 문서를 전송하기 위하여 사용 (원활한 소통을 위함)

## HTTP MESSAGE
- 구조화된 텍스트를 전송할때 사용되는 통신 규약의 메시지 즉 통신의 기본 단위이다. (http를 통해 모든 전송이 이루어지므로, http 한번이 기본 단위가 되는것)
- 클라이언트와 서버 간 데이터 교환을 위하여 사용
- 시작줄(start line), 헤더(HTTP headers), 빈 줄(empty line), 본문(body) 로 구성
  - 시작줄(Start Line) : 요청이나 응답 상태를 나타내는 첫번째줄
  - 헤더(Header): 메시지 바디를 요약하는 헤더들의 집합(general(일반) , request/response(요청/응답), representation(대표))
    - general : 요청과 응답 공통으로 사용되는 일반적인 헤더로, 메시지 전체에 대한 정보를 제공 (캐시, 연결유지 등 설정 포함)
    - request : 클라이언트가 서버에 요청을 보낼때 함께 전송되는 헤더, 요청과 관련된것들을 설명 하며 부가적인 정보 제공
    - representation : 본문과 관련된 헤더, 데이터의 혁식, 언어, 압축 등을 나타내어 클라이언트가 데이터를 올바르게 해석하도록 도와줌
    - 헤더는 서버가 요청을 처리하기 전 필요한 정보들을 제공, 또한 헤더는 미들웨어에서 처리 가능 (ex. content-Lenght 헤더가 제공하는 값에 따라 본문의 크기를 미리파악한 뒤 너무 짧거나 길면 요청 거절)
  - 빈 줄(Empty Line) : 헤더와 본문을 구분하기 위하여 존재하는 줄
  - 본문(Body) : 데이터나 문서 등 실제 내용이 포함되어 있음(ex. HTML 문서, JSON 데이터) 
- 서버에 요청을 하거나 서버가 응답을 할때 나오는 반응 그게 http message 임
  - http requset message, 줄여서 request, 한국어로 요청
  - http response message, 줄여서 response, 한국어로 응답
  - http 대표적인 클라이언트는 브라우저
- http 뿐만 아니라 많은 메시지 구성이 헤더와 바디로 이루어져 있음

## HTTP REQUEST METHOD
- 서버가 어떤 작업을 수행하길 원하는지 알리는 방법을 정의하는 명령 규칙
- 웹 서버에게 구체적인 작업을 명확하게 지시 하기 위함
- RPC : Remote Procedure Call, 원격 함수(기능) 호출
- start line에 요청 또는 수행에 대한 성공 실패가 기록되어 있음
- 요청을 보낼때 url과 get, post, put 등의 메소드를 명시하여 서버에게 작업을 요청 하는 방식으로 사용
  - GET 메소드 : 데이터 조회를 요청할때 사용 (call 적인 조회를 할때에는 요청 파라미터 사용) 
      - 서버에게 데이터를 요구
      - 클라이언트가 추가 정보를 보내는 방법
      - 요철 파라미터를 쿼리 스트링(Query String) 형태로 URL에 포함하여 전송 (본문이 없음)
        - ex. GET/ weather?city=seoul 을 입력하면 city 파라미터 값을 받아 서울의 날씨 정보를 응답으로 보낼 수 있음
      - 어떤 리소스(데이터)를 요청하는 것이기에 변경 작업에서 사용 하지 않음
  - POST 메소드 : 서버에게 데이터를 전송할때 / 쓰기(생성)
    - get 은 본문이 들어가지 않는 형태/post 본문이 포함 (요청 바디에 데이터를 첨부하여 전송한다)
    - 서버의 상태나 데이터 변경이 가능하다
    - ex. POST/user
  - PUT 메소드
    - 리소스(데이터)를 전체 업데이트할때 사용하는 메소드
    - 요청 바디에 데이터를 첨부하여 전송
    - ex. PUT/files/1
  - PATCH 메소드 
    - 리소스(데이터)를 부분적으로 수정하거나 업데이트할때 사용하는 메소드
    - 요청 바디에 데이터를 첨부하여 전송
    - ex. PATCH/users/1
  - DELETE 메소드
    - 서버에서 리소스를 삭제할대 사용하는 메소드
    - URL에 삭제하고자 하는 리소스의 식별자를 포함하여 전송
    - 서버는 리소스 식별 이후 삭제
    - 식별자는 대체로 숫자, 문자열 혹은 두가지의 조합으로 이루어지며 리소스마다 고유해야함
      - post/123 users/123 이렇게는 괜찮지만 post/123, post/123 은 안된다. 
    - ex.DELETE/users/1

## HTTP STATUS CODE (무조건 암기)
- 요청의 처리 상태를 나타내는 코드
- 통신 결과를 웹 서버가 클라이언트에게 명확히 전달하기 위하여 사용
- 요청 처리 결과를 나타내는 http 상태 코드를 응답 메시지의 헤더에 포함시켜 클라이언트에게 전송하는 방식으로 사용
- 번호대
  - 1xx: 정보 메시지
  - 2xx: 성공
  - 3xx: 리다이렉선 (도착 주소 재지정)
  - 4xx: 클라이언트 오류
  - 5xx: 서버 오류
- 중요 번호
  - 200: 성공
  - 201: 성공, 새로운 리소스 생성
  - 202: 요청을 수신했지만 처리 할 수 없었음 (서버가 요청을 처리하고 있거나 다른 프로세스가 처리중인 경우)
  - 204: 헤더는 유효하나 요청에 대해 보내줄 수 있는 내용이 없음
  - 400: 잘못된 문법으로 인해 서버가 요청을 이해할 수 없음 (Bad Request)
  - 401: 인증되지 않은 접근
  - 403: 인가되지 않은 접근
  - 404: 요청받은 리소스를 찾을 수 없음
  - 429: 지정된 시간 안에 너무 많은 요청을 보냄
  - 500: 서버가 처리 방법을 알 수 없음 (Internal Server Error, 내부 서버 문제)
  - 501: 요청 방법이 서버에서 지원되지 않음
  - 503: 서버가 요청을 처리할 준비가 되지 않았음 (일시적인 불가능)
- [그외 상태 코드] (https://developer.mozilla.org/ko/docs/Web/HTTP/Status) 
- 가령 음답으로 201이 나갔다는 것은 post 메소드의 요청이 들어왔다는것 (그렇다고 201이면 무조건 post 인것은 아님, 걍 대부분의 케이스)

## URL
- 리소스의 위치와 종류를 나타내는 주소
- 웹 상의 리소스를 식별하고 접근하기 위하여 사용
- http << 스키마/스킴(프로토콜) 즉 맨 앞에 http가 있으면 http 프로토콜을 사용하는것
- www.example.kr:80 << 도메인/호스트, 뒤에 붙는 숫자는 포트 
  - 포트는 일종의 게이트를 생각하면 된다. 80의 경우 디폴트이므로 생략하는 경우가 많다 
- 리소스 경로: /play
- 쿼리 스트링(문자열)과 경로 변수
  - 경로 매개변수 : 주소창의 경로 자체에 데이터 값을 넣는 방식(users/123/books/45)
  - 질의 매개변수 : url의 맨 끝에 ? 를 붙이고 key=value 형태로 나열하는 방식 (?category=it&sort=price&page=2))

# 파이썬 웹 서버
- web application : 서버에서 동작하는 웹 http 앱
- wsgi(실제 구현체: gunicorn) : cgi의 파이썬 버전
  - web framework - web server (웹 앱 프레임워크와 웹 앱 서버는 다른가, 그게 아니라 맞는게 따로 있음)
  - 파이썬 웹앱프레임워크: flask, django, apcher server, Nginix
  - 계속해서 변하는 데이터와는 다르게 웹 서버는 정적이었음 이를 개선하고자 나온것 (첫 웹 앱 서버는 C로 만들어졌다)
  - 웹 서버 - CGI - C서버
  - 비동기의 지원이 약한편
- ASGI (비동기 지원하는 WSGI, 이게 요즘 대세)
- 파이썬 버전 
  - brew install python
  - python.org
  - conda install python
  - but 버전이 여러개면 헷갈리니 사용하는것 가상환경 (venv, 의존성 해결)
- uv: 의존성과 버전의 관리를 용이하게 사용하기 위해 쓰는 툴
  - uvicorn
    - pip은 사용정 가상환겅(venv) 지정을 했어야 함
    - uv add uvicorn 하는 순간 빠르게 파이썬 3.14.5를 설치 한 뒤 자동으로 .venv 를 생성하고 유비콘을 설치함 (소스는 해야댐!!)
- 프레임워크, 라이브러리, 툴킷
  - 틀이 도움되는건 맞는데 틀에 너무 의존하면 너무 틀에박힌 개발만 하게 될 수 있음
  - 프레임워크: 기본적인 기능을 개발자가 짤 필요 없음 ( = 라이브러리)
- 유비콘을 좀 더 스타일리시 하게 만든것 : starlette (스탈릿)
  - 실행할땐 유비콘 uvicorn main:app

# JSON(제이슨,제이손,JavaScript Object Notation)
  - 데이터에 비정형 텍스트를 넣고 싶지 않고 구조화된 정형 데이터를 넣기 위하여 사용
  - 언어 독립성이 있어 다양한 프로그래밍 언어로 작성되어도 사스템간 데이터를 쉽게 주고받을수 있게 함
  - 읽고 쓰기가 용이하며 데이터 교환도 손쉽다
  - 이 구조화된 데이터를 표현하는 방식 (메시지 포맷)
  - 대괄호로 표현 됨, [{"키":"값"}, {"키":"값"}, {"키":"값"} ...]

# Fast api
- 비동기를 지원하다보니 django 와 같은것보다 훨씬 거 빠른 속도와 간결성을 보유하고 잇음
- 현대적임 
- api 제작에 최적화 되어있음 굉장히 효율적
- app = FastAPI() >> 인스턴스 생성 (선언)
- 프레임워크로 작성한 코드는 코드의 흐름 이해가 어려움 (제작자가 아니기 때문)
- 파이썬 자체의 능력을 기르기에는 프레임워크 남용은 좋지 않음
  - reinvent the wheel : 기능이 있는데 직접 자신이 짜는것 (비추 xx 하게되는 경우가 있다) <-> 프레임워크,라이브러리
  - 개발 자체에 대한 이해도는 낮아지는 대신 생산성을 얻는것 (reinvent the wheel 은 정반대 케이스)
- 프레임워크에 의존하지 말것
- api 문서는 공식화 되어있다 : 스웨거 문서 자동 생성
  - 개발할때 문서 필요하면 사이트 보내주면 됨 (문서 자동 생성, fastapi 만의 기능)

# rest api
- representational state api : 자원을 이름(대표)으로 식별 하고, 데이터를 구조화하며, 그 구조화된 데이터를 서버와 클라이언트간에 교환하는 방식
  - 어떤 이름으로 할건가
  - 어떤 양식으로 할건가
  - 어떤 방식으로 교환할건가
- 정보 주고 받을때 rest 방식이 아니라 grpc 방식으로 해도 됨(말 그대로 스타일임)
- 누가 논문 보고 해석해놓은거 걍 쓰고잇는거라 사용자마다 스타일이 다를 수 있음 (제작자가 실제로 소프트웨어 아키텍처 스타일이라고 해둠)
  - 하지만 restapi를 쓸때 함수 이름처럼 쓰지 말자 (함수처럼 짜는건 rpc)
    - ex. /users(rest api), /getUsers(rpc 방법)
    - 행위는 http 메소드로 묘사
    - 서버의 상태를 바꾸는건 post 아닌건 get
      - ex. 이메일을 보내는건 데이터 변화는 없지만 I/O 화가 있으므로 영향이 생겨 post
      - ex. 사용자의 상태가 미인증에서 인증으로 바뀌기 때문에 post
      - ex. 사용자 인증 조회는 get (영향을 주지 않음)
      - 그러나 이런것들도 프로젝트들을 하면서 정하는 경우가 많다
## rest 아키텍처 스타일
- uniform interface : 모든 api가 일관된 방식으로 통신하도록 하여 사용하는데 있어 단순성과 상호 운용성 보장
- client-server : 클라이언트와 서버가 독립적으로 구분되어 있어 각각의 개발 확장이 편하며 사용자 인터페이스와 데이터 저장소가 분리 되어야 함
  - ex. 사용자가 게시글을 작성할때 브라우저는 글 작성 관련된 인터페이스만 제공하고 데이터 처리와 저장은 서버에서 이루어짐
  - 독립적이므로 사용자 인터페이스 변경 없이도 서버 측 기능 업데이트 가능
- stateless : 서버는 클라이언트의 상태 정보 저장 x, 각각 요청이 독립적이며 필요한 정보가 포함되어야 함
  - ex. 온라인 쇼핑몰에서 장바구니에 상품을 추가할때 사용자 ID와 상품 정보만 사요 (사용자 정보 전부 전송 xx)
- cacheable : 응답은 캐싱이 가능하도록 명시
  - ex. 뉴스 목록과 같은 것을 사용자의 브라우저에 임시로 저장해두었다가 요청이 있을때 서버가 아닌 캐시에서 정보를 가져옴 (속도 업)
- layered system : 클라이언트는 서버가 단일 서버인지 다중 계층인지 알아서는 안됨
- self-description : 메세지 자체의 정보만으로도 클라이언트가 어떻게 처리해야 할 지 알 수 있어야 함
  
- 이름: url 경로 패턴
- 교환 방식: http 메소드
- 메시지 형식: json
- post 방식만 익히믄 된다

## 기타
- 혹시 면접에서 개념 대답을 못했다면 추후 질문시간에 걍 답 물어보기
- 알고있는데 기억 안나는거면 잠시 시간을 주쇼 하기 
- if Q. http 수정 메소드가 난다 (모르는 개념)
  - A. ㅈㅅㅈㅅ 존재한는건 기억이 나는데 이름이 기억이 안남
  - A. update 아닌감 ㅎㅎ
  - 고민을 하다보니 떠오른 경우 말하면대
- 팀 플젝 시작하면 초보의 모임이라 걍 다죽자됨
  - ai : 백엔드(서버) 초보, 문제가 생기면 모룸..
  - 백 : 클라이언트 초보
- pydantic : 으마무시함 진짜 필수 라이브러리 수준
  - 근데 우리는 그 수준은 아니라 지금 깊게 안들갈거임
- 백엔드 엔지니어 : 암기 / AI 백엔드 엔지니어 : 암기 or 검색 (코테 준비하려면 암기뺑이쳐라)


## 위클리 챌린지
- 커뮤니티 게시판 백엔드 만들기
  - 인증은 하지 마셈 (어려움), 앞으로 할 ai 서버는 대개로 인증이 필요 없음 (server to server , 내부에서만 트래픽을 받음)
  - 게시글 작성: 제목, 내용, 이미지는 제외
    - POST/posts
    - {"title":"제목", "content":"내용"} - json
    - 응답 성공 200, 생성된 글의 번호 {"post_id: 3}
  - 글 목록
    - GET/list - rpc 스타일
    - [{"id":"3", "title":"제목, "content":"내용", .... }]
  - api 아마 3~4개 나올것 같다고 하시네..
- http 내용 정리 (스스로가 생각하는 http, 요약보다는 개념 확립)
  - http 항목 전체
  - json
  - rest api
- main.py 가 커질것
- 서버를 재시동하면 삭제되지 않도록 메모리가 아닌 다른 좀 더 오래가는 저장소에 저장 필요
  - 걍 냅다 파일에 저장하기 (json, csv)
  - dbms 에 저장하기 << 이거 추천 (rdbms)
    - 개인프로젝트는 필수 아님
    - 팀 프로젝트는 필수임
      - Rdbms : 백엔드, 클라우드
      - 백엔드가 다루는지는 팀마다 다름
    - 회사 나가서 우얄겨 ai 면 rdbms 다룬다고 생각한텐데
      - 회사의 jd 열심히 보시고 필수 요구사항에 rdb 있으면 할줄알아야댐
      - 
- 백엔드는 결과물 가시성이 없다 그러나 프론트 해야할건 없다
  - 테스트 방법 : 유니테스트/E2e
    - 스웨거 문서는 e2e (request, response 확인 가능)
- 클라이언트 개발자 (풀스택 백) - 서버 개발자(인공지능)

### 한줄 요약
[한줄 요약]
#### http
- 파일을 다른곳에 보낼때 사용하는 통신 규약 즉 웹 서버와 클라이언트가 글이나 그림과 같은 데이터 주고받을때 소통의 오류가 없도록 정해놓은 규칙
#### http-message 
- 서버와 클라이언트가 데이터를 주고 받는 기본 단위
#### http-request method
- 클라이언트가 서버에게 어떤 작업을 수행하길 원하는지 명령하는 규칙, 서버에게 작업 내용을 명확하게 지시하기 위하여 사용
#### http - status code
- 받은 요청의 처리 상태를 나타내는 코드, 서버가 클라이언트에게 결과를 명확하게 전달하기 위하여 사용
#### http - url
- 웹의 고유 리소스 주소 즉 url 을 통하여 리소스의 위치와 종류를 나타냄 그렇기에 웹상에서 리소스를 식별하고 접근하기 위하여 사용
#### 네트워크 - TCP/IP
- 컴퓨터 간 통신을 위한 표준 규칙과 절차, 신뢰성 있는 전송을 지원, 통신 표준화 및 확장성, 신뢰성 그리고 표준 규약중 인터넷의 근간을 이루는 프로토콜 스택이라는 이유로 TCP/IP를 사용함
#### fast api
- api를 빠르고 쉽게 만들 수 있도록 해주는 파이썬 프레임워크 다만 서버가 없기에 ASGI 서버를 사용한다
#### rest api
- 서버와 클라이언트가 데이터를 교환할때 이름, 구조화 방식, 교환 방식 등을 말함, 아키텍처 스타일이기에 정해져있는것은 없지만 가이드라인은 있음, 통신 규칙을 통일 시킴으로써 업무 효율을 높이고 url 구조를 단순화해 목적을 쉽게 이해 할 수 있도록
#### json