# 졸음 해결법
- 야간형 보다는 조간형
  - 1시 전에 주무데요
- 충분히 씹어서 밥 먹기
- 아침광 보기
- 해외로 째고 싶으면 연락해라 나 전문가다 ㅋㅋ

# 데이터 베이스
- data + base : 자료 + 토대
- 자료를 정보로 활용하기 위하여 구조화 해놓은 데이터 모음
- 데이터가 모여있다 (그중에서도 구조화된 데이터가 주로 모여있음)
  - 구조화가 된 데이터 == 정형 데이터 (구조, 형태, 형식이 정해졌다. 정리되었다)
  - 반의어 : 비정형 데이터
  - 그렇다면 자료를 어떻게 구조화 할것인가?
- 데이터를 "구조화" 하고 "모음"(gather) 하고 있다 
- ex. 엑셀, 스프레드 시트
- 데이터를 테이블 체계로 저장하여, 데이터의 안정성과 신뢰성을 보장하기 위하여 사용
  - 신뢰성 : 엄격한 규칙 (식별자)를 걸 수 있기 때문
  - 안정성 : 데이터를 주제별로 쪼개서 관리하기 때문에 데이터값에 변화가 생겨도 단일값만 변화하면 됨 (관련된 모든것을 바꿀 필요 x)
- RDB (DB가 아닌 RDB, Realation Data Base)
  - 흔하게 쓰는 구조화 방식이 관계형(relation)이기 때문
  - 테이블로 표현하는 (tabular)
  - 상호 연관된 데이터를 테이블 형태로 저장하고 제공하는 데이터 베이스 (주제별로 구분지어서 테이블을 만들고 필요할때 join 을 통해 관계를 연결지음)
    - ex. 고객 데이터/가게 데이터/주문 데이터 를 구분 지어 놓고 고객 데이터에 존재하는 고객이 주문을 하면 각 테이블에서 필요한 값을 꺼내 씀 (통합적으로 저장하는게 x)
    - 가로 : 행(row), 세로 : 열(column)
- DBMS (db와 같은의미로 쓰는 경우도 있지만 구분 짓는 경우도 있다)
  - ex. 엑셀, 스프레드 시트
  - RDBMS : 관계형 데이터베이스 관리 시스템

# 정규화
- normalization, regularization, canonicalization : db 에서는 normalization
- 규정에 맞는 정상적인 상태로 변화, RDB의 설계에서 중복을 최소화하게 데이터를 구조화 하는 프로세스 (중복이 많을수록 데이터 관리는 어려워짐)
- 무결성을 유지하고 저장 용량을 줄이기 위하여 사용
- 상황에 따라 반정규화를 하는 케이스도 있음
- 이상현상 (Anomaly, 어나멀리)
  - 설계가 잘못되었을때 발생하는 문제점
  - ai 에서 이상현상 탐지를 위해 자주 나옴 (AD, Ai Detection)
  - 이상현상의 종류
    - 삽입 이상: 데이터를 삽입할때 원하지 않는 정보를 입력하는 경우 (ex. 새과목을 추가할때 학생을 필수로 입력해야 하는 경우)
    - 삭제 이상: 관련된 정보까지 같이 삭제되어 원하지 않는 정보까지 삭제되는 경우
    - 갱신 이상: 데이터 중복으로 인해 일관성이 없어지는 케이스
  - 이를 없애기 위하여 테이블을 나누는 작업 즉, 정규화가 필요
- raise_for_status : 200이 아니면 예외 처리로 넘어가라는 메소드 
- 정규형
  - 제 1 정규형(1NF) : 모든 필드에 오직 하나의 원자값(분리 불가능한 값)만 저장된 상태
    - 필드 안에 2개 이상의 값이 들어가있으면 1NF 위반 
  - 제 2 정규형(2NF) : 기본키가 여러개(복합키) 일때 기본키의 일부만으로 다른 속성이 결정되는(부분적 종속) 상태를 제거한 상태
    - 가령 장바구니 테이블은 id와 product_name이 복합키라 가정하였을때 product_price는 id 가 없어도 테이블의 값(속성)이 정해진다 (부분적 종속성)
    - 장바구니 cart_table, 제품의 정보 (가격과 같은것들이 들어감)가 들어가는 테이블, 사용자 정보를 담는 테이블로 구분 
  - 제 3 정규형(3NF) : 기본키가 아닌 속성이 다른 기본키가 아닌 속성에 의해 결정되는(이행정 종속) 상태를 제거한 상태
    - 2 정규형과 유사한 케이스이나 그 속성을 정하는 주체가 키가 아닌 케이스
    - 2 정규형에서 들었던 테이블을 예시로 들어보자면 장바구니 테이블에서 사용자의 이메일에 따라 주소도 정해짐 (사용자의 정보니까 즉, 종속성을 가짐)

# SQL (Structured Query Language) == 시퀄
- 구조화된 질의 언어
- 데이터베이스 시스템에서 자료를 처리하는 용도로 사용되는 구조적 데이터 질의 언어
- 데이터베이스의 데이터를 조회,추가,수정,삭제하기 위하여 사용
- 인공어(컴퓨터 대상) 
  - 프로그래밍 언어 : python
  - 쿼리(질의) 언어 : SQL
  - 마크업 언어 (마크다운) : HTML
  - ai 엔지니어의 sql 능력은 job description 을 확인해라
    - ex. 무신사는 job description에 sql 능력을 요구하지 않음
    - 데이터를 다루는 능력 자체가 default로 느껴지기 때문
    - ai 엔지니어 = 데이터 엔지니어 + 백엔드 엔지니어
- DDL: Data Definition Language
  - 데이터베이스의 구조를 만들거나 변경하는데 사용
  - ALTER(구조 변경), ADD, AFTER(기존 열 다음 지정 디폴트는 마지막), MODIFY(데이터 유형 변경), DROP(삭제)
- DML: Data Manipulation Language
  - 데이터를 조회, 추가, 수정, 삭제하는데에 사용

#  DataBase 기초
## ERD
- 개체 관계 도표, 개체와 관계를 한 눈에 알아볼 수 있도록 그려놓은 도표
- 데이터베이스의 구조를 시각적으로 표현하여 데이터간의 관계와 구조를 명확히 함 (설계도)
- 팀 플젝때는 아마 무조건 사용할것
- 데이터베이스 구조를 설계할때 엔티티와 속성, 관계를 식별하여 다이어그램 표현
- ERD 표기법
  - 까마귀 발 표기법(Crow's Foot Notation)
  - 피터 챈 표기법 (Peter Chen Notation)
  - 바커 표기법 (Barker Notation)
# INDEX
- 테이블에 대한 조회 동작의 속도를 높여주는 자료 구조
- 인덱스를 사용해 데이터를 정렬하면 매우 빠르게 정렬된 결과 출력 가능
- 인덱스가 없으면 풀스캔을 해야함 (엄청 느림)
- 속도를 높이는 방법 : 트리
- 클러스터형 인덱스 vs 비클러스터형 인덱스
  - 클러스터형 : 사전 (인덱스가 정렬됨)
    - 테이블 당 하나만 만들 수 있음
    - 테이블 생성시 기본키를 지정하면 자동으로 생성됨
  - 비클러스터형 : 목차
    - 실제 데이터는 정렬되어 있지 않음
    - 데이터가 존재하는 위치를 정리해놓은 일종의 요약본을 생성
- 다만 텍스트에는 인덱싱이 맞지 않음 (Full Text Index의 필요성)
  - 텍스트 검색용 검색 엔진이 필요하다 (ElasticSearch : maybe 클라우드분들이 할것)
  - 동작 방식
    1. 인덱스 설정
    2. 기본 검색
    3. 정밀 검색
    4. 쿼리 실행

# Transanction : 면접 단골 주제
- DBMS 혹은 유사한 시스템에서의 상호작용
- 여러개의 데이터베이스 연산을 하나의 작업 단위로 묶어 처리하는것
- 묶여 있는 연산들이 전부 다 성공해야만 결과를 반영함, 하나라도 실패시 실행 이전으로 롤백
- 무결성과 일관성을 보장하고 여러 작업을 묶어서 처리하기 위함 (논리적으로 구분)
  - 트랜잭션의 4가지 특징 (ACID)
    1. Atomicity : 원자성, 트랜잭션은 완벽하거나 아니거나 
    2. Consistency : 일관성과 정합성(무결성과 유사, 모순이 없는 상태, 사과가 전부 신선하면 무결 사과가 네임택과 포스기의 가격이 다르면 정합성 오류)이 보장되어야만 함
    3. Isolation : 독립성, 다수의 트랜잭셩이 연산되고 있어도 다른 트랜잭션이 또 다른 트랜잭션에 껴들기 불가
       - Isolation level : 트랜잭션간 데이터 간섭을 어마나 허용할것인가, 서로의 작업물을 어디까지 못보게 할것인가
    4. Durability : 지속성, 트랜잭션이 성공하면 영구적으로 반영되어야 함
# NOSQL
- sql 안쓰는dbms
- 데이터의 다양성, 대규모 데이터 처리, 수평 확장 및 유연한 설계 요구에 대한 필요성이 증가하고 잇음
  - 데이터의 다양성 : 반구조적 데이터, 비구조적 데이터 처리 가능 (구조적은 당연히)
  - 대규모 데이터 처리 : 분산 저장을 하여 병렬 처리 가능
  - 수평적 확장성 : 저혐한 하드웨어 활용 가능, RDBMS 대비 비용 효율성이 높으며 클라우드에서 실시간 확장 가능
  - 유연한 설계 및 개발 속도 : 설정의 변경이 간단하며, 빠르게 적용 가능
  - redis, mongodb, cassandra, neo4j 가 있음


# 과제 2.3
- 과제에 DB 적용
- 파이썬 안에 db 가 잇어!!

# 단체 미팅
- 구조가 어려웠던듯
  - 정답은 잘 없다
  - 대신 품질을 챙기자 
  - 학교 수업은 코테의 일부만 함
- 노트 정리 tip
  - 내가 배운 지식이 나의 지식인가?
  - 보고-따라하고-이해하고
  - 그 이후에 백지에서 작성하는것을 선호
    - 이게 해당 주제에 대한 본인만의 설명 방식이 됨
    - 노트 테이킹 (나의 생각으로 정리하는것) - 여기에 시간을 쏟아야 함
- 스크럼 시간
  - 활용 하려고 노력하지 마라
  - 정해진 활동 외에 뭘 하려고 한 팀은 딱히 본적은 없다
  - 심도 있게 얘기, 특히 blocker
- 영어 스피킹 공부
  - 이공계는 필요 없어 ^^
  - 영국은 IELTS 
  - 학기 시작하면 걍 미리가서 많이 말하면 알아서 늘음