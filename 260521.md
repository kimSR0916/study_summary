# 구조개선
- 아마 현재는 main.py 단일 파일 하나일것
- 스스로 깨닫고 찾고 느끼고 하는 경험이 매우 중요
  - 내가 알아야 선택과 집중이 가능
  - 남의 경험을 잘 받아들여야 하는 방법을 알아야 함

# 디자인패턴
- 면접 단골 질문
- 반복 x, 일정한 형태 (한곳에서만 보이는게 아닌 다른곳에서도 보이는 것, ex. 한옥마을)
- 자주 사용하는 설계 형태를 템플릿을 제작해 놓은것 (패턴 = 템플릿)
- 구조적인 문제 해결 및 재사용성을 높이기 위하여 사용
- 소프트웨어의 규모가 달라져서 구조적인 문제를 알게 됨
  - 규모 : 복잡도 (complexity)
- 코드를 적절하게 분배
- 구조 개선 == 리팩토링 (기능의 변화 없이 구조만 변화)
- route : 경로, 길/ router : 길라잡이 (요청이 오면 컨트롤러한테 넘겨줌)
  - 아키텍처 패턴 : 전체적인 소프트웨어에서 발생하는 문제들을 해결하기 위한 방법
  - 코드를 적절하게 분배하는데에 목적을 둠 (코드가 너무 길면 가독성도 떨어지고 파악이 어려워진다)
- 코어 로직, 비즈니스 로직 : 핵심 기능 코드 (컨트롤러가 이 역할을 함)
- 모델 : 데이터 관리 (데이터들의 구조, 형태를 잡음)
  - service : 규칙 적용, 처리 흐름 결정, 여러 작업 조합 (회원가입 시 비밀번호 암호화 후 저장 요청)
  - repository(DAO) : DB 조회/저장/수정/삭제 전담 (INSERT, SELECT, UPDATE 수행)
- view 는 html, css, javascript
- 데이터 베이스에서 이름을 가져오는 get 요청의 순서
  1. http 요청이 들어오면 라우트가 적절한 컨트롤러로 요청을 전달함
  2. 컨트롤러에서 필요한 데이터를 모델 에서 get 요청을 수행
  3. 모델은 데이터베이스에서 데이터를 가져온 뒤 컨트롤러로 넘김
     - 서비스에서 비즈니스 규칙에 따라 처리 한 뒤 레포지토리로 넘어감 이후 데이터베이스에 SQL을 실행하는 구조
  4. 컨트롤러는 데이터를 가지고 요청을 수행
- 주니어 - 시니어 - 아키텍트(기술사)

# 미들웨어
- middleware : web app server(FastAPI, uvicorn)
- OS - middleware - App (가장 포괄적)
- ASGI server(uvicorn) - middleware - request handler
- 공통으로 개입하는 장치 (모든 요청에 대해 다 가로채서 공통으로 개입할때 사용, ex. 시간 적용)
- 요청이 라우터에 도달하기 전/응답이 클라이언트로 나가기 전 전체 흐름을 감싸서 진행하는 코드 (즉 요청이 들어온 이후에는 죄다 개입)
- 적합한 작업 방식
  - cors 처리 (밑에서 더 자세히 설명) : 브라우저의 요청 허용/차단 판정을 위해 모든 응답 헤더에 공통 적용이 되어야 하므로 필수 수준임
  - 전역 Rate Limiting : 과도한 요청은 가능한 한 빨리 차단해야 하므로 라우터 이전 단계에서 처리하는것이 적합
  - 요청 처리 시간 측정 : 요청 시작부터 응답 까지의 시간을 측정하려면 전체 과정에 개입 가능한 미들웨어가 필요

# Exception Handler - 수업 중 설명은 없었음
- 예외 처리 핸들러
- 에러 응답 형식을 통일하고 에러 로직과 비즈니스 로직을 분리하며 코드의 중복을 제거하기 위하여 사용
- http 상태 코드를 임의로 덮어쓰면 안됨 (ex. 404를 400으로 처리하기)
- 비즈니스 로직을 exception handler에 넣기 금지 (응답변환과 전당 방식만 담당해야 함)
- 모든 예외를 정상 처리해선 안됨

# 프론트엔드
- 마크 : 문서에 추가되는 정보, 문서 언어
  - 마크업 : 좀 더 도드라지기 때문에 마크업이라고 부름 ex. <head> (html 에서는 태그 라고 부름)
- 문서를 뜯어서 이해하는 것 == parsing
- html(Hyper Text Markup Language) : 내용을 담는데에 집중
  - 시맨틱 태그: 웹페이지의 내용을 설명하는 태그, 내용이 어떤 의미를 가지는지 명확하게 표현하는 태그 (header, footer, article, section)
- css : html 나온 이후에 나옴, 문서를 시각적으로 더 보기 좋게 해줌, 인간 전문가 드물다, 디버깅이 어렵다

# JS
- next 컴퓨터 보고 싶으면 제주도 넥슨 컴퓨터 박물관 가면 볼 수 있음
- 아니면 포항공대 네트워크 랩
- 브라우저에서 코드를 실행하고 싶어서 만들어진 언어 
- 보안적으로 굉장히 취약

## Fetch
- 컴퓨터에 들어오는 지시사항을 읽고 그 지시에 따라 실행하는 첫 단계
- 정확한 작업 수행을 위해 필요한 정보를 불러오기 위하여 사용
- 프론트에서 백으로 정보를 요청할때 필수적으로 사용됨
- 통신: 서버에 접속해서 정보를 획득
## cors (교재-웹 응용에 존재, 진짜 중요)
- cross + origin + resource + policy
  - 교차 출처 자원 공유
  - 웹 페이지상의 제한된 리소스(데이터) 오리지널이 아닌 다른 도메인으로부터 요청할 수 있게 혀용하는 구조
- cors == cross-origin <-> same-origin
  - same-origin (동일 출처) : 프로토콜(scheme), 호스트 이름 (Host), 포트(Port) 가 동일한 경우 동일 출처로 간주
  - cross-origin : 프로토콜, 호스트, 포트중에 하나라도 다르면 교차 출처, cors 정책에 따라 접근이 허용된 경우에만 사용 가능
- Simple Request : 서버의 상태를 변경하지 않으므로 걍 바로 보내도 됨
- Preflight(시험비행) Request : 바로 직접적으로 넣는게 아니라 시험 식으로 좀 보낸 뒤에 본 요청을 보냄 (서버의 상태를 변경하므로)
- 접속의 통제 주체: 브라우저 정책
- 다른 오리진의 접속을 허용하겠다고 의사를 표현하는 주체 : 서버 정책
  - 님 서버 접속이 안됨 cors 뚫어주세요
  - 허용을 주는 방법은 서버 응답 헤더로 줘야 함

# Streamlit
- 프론트 엔드를 파이썬으로 짜게 해주는 프레임워크
- [링크](https://streamlit.io/)

# 위클리챌린지
- user end - frontend - ai backend - db llm end
- 우리의 역할 : bridge builder
- 인터페이스 잘알이어야 함, interface design을 하자

# 현직자 특강
- 내 코드에 매몰되지 않기
- 포트폴리오는 핵심만 (열심히 고심한 프로젝트 1,2개도 나쁘지 않을듯)
- 정체성 정하기
- 일관된 스토리
- 양보다 깊이
- 면접
  - 경험
  - 일관성
  - 전환점
  - 마인드셋
  - 스피킹 연습
  - 질문들 비슷비슷
  - 면접관의 태도를 보며 영점을 조절하자
- 우선순위 설정
- 딥다이브
- 정보는 geek 뉴스나 링크드인에서 확인
- 팀플젝에 가장 집중한듯
- 한문제를 여러 방식으로 풀이 << 이거 괜찬은것 같은데
- 클로드로 review 하기
- 공식 문서 자주보기 (best practice 중요시)
- 클로드가 흐름이 더 빠른 느낌이라서 사용 코드 작성은 클로드, 코드 리뷰는 코덱스가 나음

# 투데이 리뷰
- 다이어그램 그리고 싶으면  mermaid 쓰면 된다 (수학은 math)
- 서브노트(자신만의 노트) 만들기