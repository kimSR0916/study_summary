- 파이썬은 못하면 zep 오후 3시~5시 사이 alex 방문 꼭
- 웹 : 파이썬은 잘한다
  - cs 기초가 없거나 약하가
    - os(network) 꼭 학습 : 이번주 안으로도 가능
- 이번주는 가능하면 0,1로 위클리 챌린지


# Numpy
- array (N-D배열)
- numerical vs mathematical
  - 수학과 관련된건 기본적으로 mathematical
  - 응용 수학 (수치 해석, 유일하게 컴퓨터를 사용) = numerical
  - 순수 수학 (컴퓨터는 무슨 종이랑 펜 씀) = mathematical
- AI 엔지니어
  - 백엔드 엔지니어
  - 인프라 엔지니어
  - AI 과학자
- 배열, 행렬, 수학 사용자들을 위한 문법
- 개발자들에게는 배열, 행렬, 수학과 같은 분야의 어려움때문에 넘파이가 어렵지 코딩때문에 어렵지는 않음
- 대규모 수치 데이터를 빠르고 메모리 효율적으로 처리하기 위하여 사용
  - 과학이 많은 데이터를 다룬다
  - 넘파이는 어떠게 시간 공간 효율을 기본 파이썬보다 improve(개선) 시켰나
  - 넘파이보다 더 잘 할수는 없나?
  - 구글 코랩을 쓸것
- 데이터 처리에 가까움

# 위클리 챌린지
- 코랩 링크 irl
- 깃허브 ok
- ipynb : iPython Notebook
- 과학 계산용 언어 경쟁
  - Python
  - R
  - Julia : 빠름
  - 원조 : Fortran
- 런파임 : 파이썬 실행 + os

# 프로그래밍에서의 데어티 개념
- primitive type
  - 기본형 (simple type)
    - int
    - str
    - bool
  - 복합형(complex type)
    - collection(모임) : list, tuple, dict, set
    - class -> struct
    - c++: struct, class
      - Swift
# 과학자에게 데이터
- 데이터 : 수리과학 (mathematical science)

# 벡터공간의 차원의 정의
- 기저(집합)의 크기 (원소의 개수)
- 기저의 개수 X

# 차원
- 0차원 : 스칼라
- 1차원 : 벡터, 리스트
- 2차원 : 행렬
- 3차원 : 텐서

- 개발자는 원본보다도 데이터의 차원을 변환(transform) 하는데에 중점을 둠

# shape
- 배열의 각 차원별 요소(행, 열, 깊이) 개수를 나타내는 튜플
  - 수학에는 이런 개념이 없음
- computer is the incarnation of mathematics
- np.shape(2,3) 이면 2 는 axis = 0 부분, 3 은 axit = 1 부분
  - axis 0 : 세로축, axis 1 : 가로축

# 데이터 타입
- 넘파이 이새기 주제넘게 타입까지 해?
- C 한테 넘어가야 해서 그래용,,,
- 타입 추론(implicit type inference) : 내가 명시적으로 타이핑(typing, 타입 주기)를 안하고 컴파일러(인터프리터)가 알아서 지정하는것 
- but!! 내가 편하면 컴퓨터가 일을 더 해야 하므로 컴퓨터의 성능을 개선하고 싶다면 사용하자

# 인덱싱
- DB에서는 인덱스가 아닌 인덱싱을 사용
- 인덱싱 : 인덱스를 생성한다 (건다)는 행위를 강조할때 사용
  - 디비 느려 인덱싱 좀 해
  - 위치 : index, position, location 등 다양한 용어 사용
- 파이썬 슬라이싱 열심히 공부할것

# 연산
- 2회독 하면 진짜 조음 진짜 많이 얻어감 (추천 : 13주차)
- 요소별 연산
  - 넘파이 안쓰면 못씀

# 유니버설 함수 (universal function)

# 판다스 (팬더스)
- 데이터프레임
  - RDB에 가까움
  - 그러면 왜 RDB를 안쓰는가 : RDB 보다 너무 낮은 수준의 언어라 사퀄을 이용하려면 해야할게 너무 많음
- 하고자 하는 작업을 수월하게 하고자 하는 성향이 강함
- 넘파이와 달리 데이터 분석에 가까움
- 데이터에 대한 작업
  - 데이터 분석 : 읽기
  - 데이터 조작 : 쓰기
- 시퀄 쿼리 (검색 질의 읽기)
  - QUERY(질의) : SELECT
  - MUTATION(변경) : INSERT, DELETE, UPDATE
- 디비는 주기적으로 백업을 함 (거의 매일)
  - 실수해서 디비 날리면 싹싹 빌고나서 복구 작업 들어가기 
- 과연 판다스 > sql 일것인가
  - 데이터소스 - 원천 - RDBMS 서버 (클라우드)
    - 파이썬 프로그램 메모리에 다 올라가야 됨

# 시리즈
- 넘파이랑 비슷한 면이 느껴짐
- 데이터프레임의 구성요소로서 중요함 (데이터프레임 쪼개면 나오기도 함)
- argument : 인수

# 데이터프레임
- 줄
  - column-based : 열 기반 (pandas)
    - 열기반 데이터프레임은 여러개의 데이터 프레임이 시리즈(열)로 구성되어 있음
  - row-based : 행 기반 (rdb)
    - 하나의 테이블이 여러개의 레코드 (행)로 구성되어 있다. 
- EFFECT = no main effect + 'print' side effect(부수효과)
- pure function : 함수 주효과가 많거나 다고 부수효과는 적거나 없으면 (no siede effect)

# 필터링
- where
- db(sql) 근본 하셔야 함
- 파이썬 + C (C++, Rust)
- os (표준 unix)
- tcp/ip
- al/ds
- 위에것만 알면 어지간한 cs 다 공부 가능

# 결측시 처리
training - 학습, 훈련, 공부
validation - 학습이 잘 되었나 검증, 모의
test(eval) - 학습이 끝난 모델 테스트 : 수능
evaluation : 학습이 끝난 모델을 검사 (평가)

# 모델 만들기
- 파인 튜닝
- 합성 데이터가 잘 작동하는가

# 오늘의 한줄 정리는 전부 다~ 

- numpy : 다차원 배열 및 행렬 연산을 위한 고성늘 수학 함수와 도구를 제공하는 파이썬 라이브러리로 수학 연산, 선형대수, 통계, 변환, 기본적인 수치 계산 등이 가능하도록 함수와 도구를 포함하고 있음
  - 다차원 배열 객체를 가지고 있고 빠른 연산 속도와 브로드 캐스팅 기술, 다른 라이브러리와의 호환성 등등의 특징을 가지고 있어 대규모 수치 데이터를 빠르고 효율적으로 처리하기 좋음
- 차원 : 사물이나 공간의 크기, 범위, 특징등을 나타내는 측정 가능한 속성, 배열에서 데이터를 구성하는 축의 개수 넘파이 배열은 0차원은 스칼라 1차원은 벡터 2차원은 행렬 3차원은 텐서 그 이상은 N차원 배열로 정의하고 있다. 데이터 구조화를 통하여 연산 속도 향상, 메모리 효율성, 수학적 일관성, 코드 간결성을 확보하기 위하여 사용
- shape : 배열의 각 차원(axis, 축)별 요소 개수를 나타내는 튜플, 차원은 개수 형태는 튜플이다 배열 구조를 명확히 하여 연산의 일관성과 정확성을 보장하기 위하여 사용
- 데이터타입 : 배열의 각 요소가 가질 수 있는 값의 유형을 지정하는 속성 정수형, 부동소수점형, 복소수형, 문자열형, 불리언형, 객체형, 유니버설타입이 있다 메모리를 절약하고 연산 속도를 높이기 위하여 사용
- 인덱싱 : 배열 내 특정 요소의 위치를 나타내는 정수값, 정수, 슬라이싱, 불리언 인덱싱, 팬시 인덱싱이 있다. 배열의 특정 요소에 접근하여 데이터를 선택적으로 조작하고 처리하기 위하여 사용
- 연산 : 요소에 대해 수행되는 수학적 또는 논리적 계산, 배열 연산은 벡터화를 지원하기에 반복문이 없어도 빠른 연산이 가능하다는 특징이 있다 대량의 데이터를 빠르고 정확하게 처리하여 분석 및 계산의 정확성을 높이기 위하여 사용 됨
- 유니버설 함수 : 배열의 모든 요소에 동일한 연산을 적용할 수 있는 기능, 넘파이에서는 각 요소에 반복적으로 수행되는 벡터화된 연산을 제공하는 함수이다 계산 속도를 높이고 메모리 사용 최적화를 위해 사용됨
- pandas : 구조화된 데이터의 조작과 분석을 위한 데이터프레임 및 시리즈 객체를 제공하는 파이썬 라이브러리, 데이터를 효과적으로 처리하고 분석하기 위하여 사용
- 시리즈 : 인덱스를 가지는 1차원 배열 형태의 데이터 구조, 한개의 열로 구성된 데이터라 볼 수 있으며 각 요소가 고유한 인덱스를 가짐
- 행과 열로 구성된 2차 테이블 형태의 데이터 구조, 데이터, 열, 행, 인덱스로 이루어져 있음
- 필터링 : 원하는 신호만 통과시키는 기능을 하는 장치나 과정을 말함 시퀄의 where 절과 흡사하다
- 그룹화 : 관련된 여러 요소를 하나로 묶은 논리적 또는 물리적 단위
- 병합 : 여러 데이터프레임을 공통 열 또는 인덱스를 기준으로 결합하는 과정 서로 다른 데이터 소스를 결합하여 일관된 형식으로 통일하고 종합적인 분석을 위하여 사용
- 결측치 처리 : 데이터프레임이나 시리즈에서 누락된 값을 탐지하고 제거하거나 대체하는 작업 정확성과 신뢰성을 올려준다
- 피벗 : 데이터를 특정 기준에 따라 재구성하여 보다 쉽게 분석할 수 있도록 하는 과정
- 중복 제거 : 데이터프레임이나 시리즈에서 동일한 값을 갖는 중복된 행을 식별하고 제거하는 작업 데이터의 정확성과 일관성 유지 가능
- 문자열 처리 : 시리즈 객체 또는 문자열이 포함된 데이터 프레임에서 문자열의 조작 및 변환을 수행함, 비정형인 문자열 데이터를 보다 정확하고 일관된 분석이 가능케 함
