- 모르고 엔지니어링 하는것과 알고 하는것에는 차이가 있음
- 모른다고 엔지니어 실패~! 백수 합격~! 하는거 아님
- 2주간의 ai 기초는 이런 기회가 인생에 걸쳐 딱 한번뿐일수도 있음
- 이해하기에는 턱 없이 부족한 시간이지만 
- 들어가며는 걍 건너뛴다!!!!!
- we will use pytorch (because pytorch is the best of the making AI framework)
- kaggle is the site of the data!! please sign in
- today&tomorrow purporse : Model Architecture
- when we make ai, we need to take the process (data preprocessing) before entrance the ai
- next week we will learn about detail of the model
  - DNN modle
  - NLP Transformer
- question mark is not necessary process to study the subject

### 데이터 전처리
- 선택적인 것이 아닌 꼭 해야하는것
- AI 엔지니어 입사했더니 주업무가 데이터 전처리라 실망할 수도
  - 원유를 끌어올리는것 = 데이터 전처리하는것
  - 매 토픽마다 나오는 사용방법 잘 볼것
1. 데이터 수집
2. 결측값 처리
3. 이상치 제거
4. 데이터 정규화
    - 최소값과 최대값을 정해놓음
    - MinMaxScaler() : 최소값과 최대값을 기반으로 0과 1사이로 변환하는 클래스
5. 데이터 변환
6. 데이터 분할

### 데이터 증강
- 데이터가 부족할때 사용하거나 과적합을 방지 (이 케이스는 ㅂㄹ 없음), 일반화 성능 향상
- PIL(Python Image Library)
- return Image.fromarray(np.random.randint(0, 256, size + (3,), dtype=np.uint8))
  - fromarray : 배열
  - 0~256
  - size: 출력 배열 크기
  - dtype : 데이터타입
- def rotate_image(image, angle):
  - image : 타겟이 되는 이밎
  - angle : 돌릴 각
- from PIL import ImageOps def flip_image(image, direction):
  - 교재엔 else: resutrn image 가 없긴한디 없으면 붙이면댐
- 평행 이동
- 크기 조정
- 이미지 밝기, 노이즈 추가 (직접 해보세용^^)

### 데이터셋 분할
- 모델의 성능을 평가하고 일반화 하기 위한 과정
- 트레이닝에 의해서만 분할되는건 아님 (우리가 ml을 배워서 이렇게 배우는것뿐)
  - 학습 : 패턴 학습용 데이터
  - 검증 : 최적화 사용시 쓰는 데이터
  - 테스트 : 평가 데이터
- 손으로 하는 방법도 있고 방법은 다양함
- 전체 데이터 수집
- 데이터 분할
  - 교재보다는 train_ratio = 0.6 가 더 낫지 안을까
  - validation_data = df[train_size:train_size + validation_size] (슬라이싱 뒷부분 더하는 부분 잘 보기)
  - test_data = df[train_size + validation_size:] (앞이 없으면 0, 뒤가 없으면 -1 (끝))
  
### machine learning (머신 러닝)
- 결국 알고리즘 but 머신러닝은 자동으로 개선함 스스로 학습 함
  - 진짜 스스로가 아닌 데이터의 패턴을 학습해 예측해 내는것
  - 분류, 예측
  - ai의 가장 큰 카테고리는 ml
  - original: 규칙 기반 시스템 (사람이 모든 규칙을 직접 프로그래밍 해야 했음, 수제)
    - ml은 이러한 한계 극복을 위하여 등장
  - 복잡하거나 예외가 많으면 정확도가 떨어짐

### Random Forest
  - 의사결정트리앙상블은 지금도 쓴다
    - 너무 한 모델에만 의존하지 말자
  - 그렇게까지 자주 쓰진 않음

### KNN
- KNN 이랑 SVM 은 자주 나옴
- 분류하려는 데이터 포인트와 가장 가까운 이웃 데이터 포인트 개수
- 즉 데이터를 가장 가가운 k개의 이웃과 비교하여 분류하거나 예측함
- 거리가 굉장히 중요함
- 유클리드 거리로 할대는 x축y축 정의가 어려우므로 normalize를 해줌
- 맨해튼 거리/유클리드 거리 사용

### SVM
- 대학은 답을 잘 모르고 푸는 경우가 많음...
  - 교수님 업무부터가 답이 없는 경우가 많음
  - 세상엔 답이 있는 경우가 없음...
- 초평면 (hyperplan)
  - 2차원 평면에서는 경계선, 직선 (경계선? 에 있는 점선, 그래프 참고)
  - 3차원 공간에서는 경계면, 평면
  - 4차원 공간에서는 우리가 알 수 없음 (공식을 이용해서 구해내자)
- 마진 : 가장 가까운 초평면 사이의 거리
- 굉장히 안정적이게 데이터 분류 가능
  - 딥러닝 나오기 전에는 자주 쓰였으며 지금도 쓰임
- 데이터를 가장 잘 구분

### Naive Bayes (나이브 베이즈)
- Naive : 모든 특성이 서로 독립적이라고 가정
- 확률들을 계산해주는 소스 : 학습 데이터
- 가우시안 == 정규분포
- 특정 수치 값 전체가 따르는 모양 (분포)를 가정 == 모집단(population)을 가정
  - 대체로 이런 가정으로 풀었을때 성능이 좋았어서 걍 써왔음 ㅇㅇ (당연히 틀릴 수 있지)
- 확률적으로 데이터를 구분
- 가장 높은 확률을 가진 결과를 저장

### deep learning (딥러닝)
- 데이터를 스스로 학습하여 예측하거나 분류하는 모델을 자동으로 만들어내는 기술, 비선형적인 패턴(복잡한 관계)을 찾아내는게 핵심
- 머신러닝 (전통적) : 알고리즘을 선택하고 훈련
- 딥러닝 : 알고리즘(규칙)을 선택하는게 아니라 규칙을 설계 (규칙이 신경망 안에 들어가 있음)
- 데이터가 정말 많이 필요
- XOR : 선형으로 해결 못하는 대표적인 게이트

### perceptreon(뉴런, 퍼셉트론, 노드)
- ANN(Artificial Neural Network)의 기본 단위
- 퍼셉트론 내지는 노드라고 부름
- 딥러닝에서 제일 중요한건 이상치(error)
  - 이게 더 좋은 bias 를 만들어줌
- 결과를 받고 전달하는 단순 모델
- 가중값 + 활성화 함수

### Activation Function(비선형 활성화 함수)
- 퍼셉트론이 한개인 경우 And, or, not 연산 가능 (xor은 해결 못함)
- 선형만으로는 완전할 수 없음
- 시그모이드 : 모든 함수값을 0과 1 사이에 가둬둠
- xor 같은 선형으로만은 해결 못하는 데이터를 해결하기 위함
- 노드 들이 연결되어 있고 그 연결되어 있는 선이 하나하나 다 네트워크 이며 그렇기에 뉴런 네트워크이다
- bias 가 0인 케이스도 있음

### fcnn (fully connected neural network)
- 모든 뉴런이 연결되어있음
- 요즘 네트워크들 다 이러긴 함...

### fcl (fully connected layer)
- 모든 입력 뉴런이 모든 출력 뉴련과 연결된 레이어 
- 입력 데이터의 모든 특징을 종합적으로 고려하여 복잡한 패턴을 학습하고 최종 출력이나 분류를 수행할 수 있기 때문
- simplecnn 을 생성할때 초기화 하는 모듈을 작성해야 함
- linear : 선형계층, 황성화 함수, 가중함 (선형 결합)
- 두번째 레이어의 인풋 개수는 첫번째 레이어의 아웃풋 개수가 되어야함 (fcl 이니까)
- forward : 재정의해야함, 레이어링(연결 순서)을 잡아줌
- flatten : 다차원 데이터를 1차원데이터로 만들어줌

### loss function (손실 함수)
- 예측 값과 실제 값의 차이를 정량적으로 측정 하는 함수 (양으로 나와야됨 좋다 안좋다 안됨)

### backpropagation (역전파)
- 손실 함수를 줄임 == 최적화
- 답이 없는 질문일 경우 내 대답이 좋은 이유를 내가 주장해야 함
  - 제발 정답은 아니지만 받아들여줘 라고 싹싹빌기
- 설명용 그림 잘 볼것 (이번주 토요일 끝날대까지 이 그림 특히 3,4단계 완벽히 이해해오기)
  - 남한테 설명할 수 있을것
  - 0/1단계 상태로 코드 짤 수 있을것
- 순전파(feed-forward)를 통하여 손실함수를 하용하여 오차를 계산
- 역전파를 통해 손실함수에 대한 가중치의 기울기를 계산하여 최적화로 가중치 조절

### 한줄 정리 32번까지
- 데이터 전처리 : 데이터를 분석 및 모델 학습을 위해 원본 데이터를 정제하고 변환하는 과정으로 결측값 처리/이상치 제거/데이터 정규화/데이터 변환 (보통 6:2:2) 등이 있습니다 데이터 전처리는 모델의 성능을 향상 시키고 신뢰할 수 있는 예측을 위해 데이터를 정리하고 데이터를 최적화 하기 위하여 사용합니다
- 데이트 증강 : 머신러닝을 위해 기존 데이터를 변형하여 새로운 데이터를 생성하는 기법으로 회전, 크기 조걸, 색상 변화, 노이즈 추가 등의 방법을 사용하여 다양한 형태의 데이터를 제작합니다. 이를 통해 과적합을 방지하고 일반화 성능을 향상시킬 수 있습니다
- 데이터셋 분할 : 데이터를 목적에 맞게 학습, 테스트, 검증으로 분할하여 모델의 성능을 평가하고 일반화하기 위한 과정입니다. 과적합을 방지하고 성능을 객관적으로 평가하며 일반화가 잘 이루어지는지 판단하기 위하여 사용됩니다
- 머신 러닝 : 데이터 속의 패턴을 학습해 예측하거나 분류하는 프로그램입니다 자동 학습을 통하여 예측을 개선하고 복잡한 문제를 해결하기 위하여 사용합니다.
- 랜덤 포레스트 : 의사결정트리 기반의 앙상블 학습 기법으로 다수의 트리를 결합하여 예측 성능을 향상 시키는 모델입니다. 여기서 트리는 각자 독립적으로 학습됩니다. 노이즈에 강하다는 특징이 있습니다.
- k-nn : 새로운 데이터를 가장 가까운 k개의 이웃과 비교하여 분류하거나 예측하는 알고리즘 입니다. 학습 과정에서 별도의 파라미터를 학습하지 않고 예측 시정에 직접 이웃을 찾는 특성이 있습니다
- svm : 주어진 데이터를 최적으로 분리하는 초평면을 찾는 지도 학습 알고리즘 입니다 여기서 초평면은 2차원 기준 두 그룹을 분리하는 선을 의미합니다
- 나이브 베이즈 : 모든 특성이 독립적이라 가정하고 베이즈 정리를 활용하여 확률을 계산하는 분류 모델입니다 이러한 가정 덕분에 계산은 단순해지는데다가 독립성이 없는 경우에도 좋은 성능을 보이는 경우가 많아 자주 사용됩니다.
- 딥 러닝 : 데이터를 스스로 학습하여 예측하거나 분류하는 모델을 자동으로 만들어내는 기술입니다 머신러닝과의 차이점은 사람이 직접 규칙 즉 알고리즘을 지정해주지 않아도 된다는 점이 있습니다
- 퍼셉트론 : 인공신경망의 기본 단위로 입력값을 가중치와 함께 처리하여 단일 출력을 생성하는 선형 이진 분류기 입니다. 즉 함수 내부에 조건문이 있는 여러 정보를 받아 판단하고 결과를 전달하는 방식을 단순하게 만든 모델입니다.
- activation function : 인공신경망에서 뉴런의 출력을 결정하는 비선형 함수입니다 노드가 입력 신호를 받으면 이를 비선형 함수에 적용하여 최종 출력을 생성하는 역할을 합니다
- artificial neural network(인공신경망) : 뇌의 뉴런 네트워크를 모방한 알고리즘으로 입력층과 은닉층 출력층으로 구성되어 있으며 층 사이사이에는 가중치값들이 행렬로 이루어져 들어가 있습니다.
- fully connected neural network (완전 연결 신경망) : 모든 뉴런이 이전층의 모든 뉴런과 연결된 신경망 구조로 입력 데이터의 모든 특징을 활용하기에 복잡한 패턴을 학습하고 예측하는데 효과적입니다.
- fully connected layer (완전 연결 계츨) : 인공 신경망에서 모든 입력 뉴런이 모든 출력 뉴런과 연결된 레이어로 FCNN은 입력층에도 영향이 가는데에 반해 FCL은 입력층은 제외한다는 특징이 있습니다 기능은 FCNN과 유사합니다
- loss function (손실함수) : 예측값과 실제 값 간의 차이를 정량적으로 측정하는 함수를 의미합니다. 실수값으로 표현되며 손실함수의 값과 예측 성능은 반비례합니다
- backpropagation (역전파) : 손실함수를 기반으로 각 뉴련의 가중치를 조절하기 위하여 사용하는 알고리즘입니다. 모델의 예측 성능을 최적화 하기 위하여 사용됩니다

# today review
- 기술의 사용자 관점에서 볼것 (우리는 과학자가 아니다)
- 주기적 remind를 해드림
- FCL : 레이어 한 층에 대한 설명 
- FCNN : 전체 네트워크에 대한 설명
- 월요일은 가능하면 빠지지 말것
- 마인드맵 만들면 조을것 같어
  - 사실 마인드맵 말고 토픽별로 요약하는 이미지를 만들어볼까나 (흐름도도 ㄱㅊ)
  - ai를 써봥
- 내일부터 주석도 같이 쓰면서 강의를 나가겠습니다
- DL은 탑다운 형태로 작성, 복습 함 해보셈
- 내 것이 되는 과정
  - 복습
    - 교재 토픽, 본문 코드 (타이핑 전부 다)
    - 선택 : 미니퀘스트
  - 위클리 챌린지