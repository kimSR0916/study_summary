- 모델을 만든다
  1. 데이터 필요
  2. 모델 설계
  3. 모델 생성(초기 가중치)
  4. 모델 학습
  5. 모델 검증
- 데이터를 어떻게 구할것인가?
  - kaggle
  - 사이킷런
  - hugging face
- 모델을 만들때 pytorch 라는걸 사용해서 만들것
  - 클래스를 작성해주자
  - class TestNN() #슈퍼클래스
  - init, forward << 첫번째 인자가 다 셀프이다
    - init에는 레이어 설계
    - self.layer1 = Linear
    - self.layer2 = 
    - forward
    - relu(self.layer1(x))
- 가중치 업데이트
  1. forward
  2. 손실함수 값 label - output
  3. back(역전파) -> gradient
  4. 최적화 : 가중치 업데이트 (optimizer, adam)
     - optimizer 아셔야 하기에 목요일에 합니다

#### 경사 하강법
- 가중치를 반복적으로 조정하는 최적화 알고리즘

#### convolutional
- 윤곽
- convolution : 합성곱
- convolutional mask : 필터
  - 이미지 전부를 보는게 아니라 일부값으로 스캔을 하는것 (표본 뽑기)
- pooling layer : 고르는건 ㄹㅇ 내맴
- stride : 보폭, 즉 몇 픽셀씩 이동할것이냐 나타내는것
- 마스크인 커널은 전체의 크기를 넘지 않음

#### flatten layer
- 다차원 배열을 1차원으로 바꾸는것
- feature extraction
  - cl
  - classification
  - flatten
  - fcl
  - fcl

#### todat review
- 앞으로는 수업 노트도 코랩에
- 디테일 전달은 심화보충에서
- 코드를 보자 코드를 코드를 직접 쳐봐야해
- 멘토링 내용 공유
  - ai be vs fullstack be
  - 백엔드 파이썬으로 넘어오는 추세
  - 파이토닉한 코드를 짭시다
  - api llm + local open weight llm, 서버를 파고 운영을 하고