#### 이번주
- 토요일 수업 이번주까지만
- langchain 이런것들은 agent frame work 라고 부름
- 회사에 들어갓더니 자체 ai 프레임워크를 쓰는 경우가 잇음
- ai는 개인교사(튜터) << 여기 까지는 괜찮음
- 그렇다면 만드는 일에 있어서 AI의 역할
- 사람 : 코드를 작성하는 역할이었음 (코더, 프로그래머, 개발자)
- 지금은 ai가 코드를 작성하는 역할, 그러면 사람은 지금 검수의 역할을 하는가
- 기초가 부족한데 시간이 없다!!
  - 바텀업은 더더욱 없다!! (대학원은 대놓고 탑다운)
- 기초는 충분할것
- 슬슬 개인 프로젝트 준비'
- 문제를 풀때는 특정 아키텍처에 의존하기 보다는 여러 아키텍처를 읽어보기
- 재귀 이해하는것 중요

#### AI는 도구 - 리누스 토발즈
- ai를 써서 개발하는 이 과정은 위탁이 아닌 나의 아웃풋을 향상시켜주는 도구로 바라보자
- 시스템의 복잡성을 히애하는 사람은 도구를 잘 씀
- 시스템 전체의 흐름을 이해할 수 있었다 : 파이프라인
- 직접 코딩 하는 경험을 갖기가 어려워지고 있음 (이것의 극복 방법이 현재 최대 논제)
  - ai가 코드를 작성하면 꼭 설명하는 문서도 같이 작성하도록 시킬 것
  - 코드와 문서를 같이 
- 탑다운 방식이 짱임
  - 어려운 이유
    - 각자 하고 싶은 바가 다름
  - 이제 탑다운이 가능한 이유 : AI
  - 회사 가서도 공부하는 방법임, 탑다운으로 배우는 방법을 훈련 합시다

#### 모델 architecture
- 모델이란것은 어떤식으로 구성되어있나
- 모델 == (파라미터==가중치 의 집합)
  - 수학으로 봤을때에는 함수이다
- 모델 아키텍처 : 그 가중치들이 어떤식으로 구성되어서 어떤 일을 하고자 하는가
- 앞으로 해야할 역할은 문제 해결자(problem solver)
- 가중치 업데이트 : 옵티마이저
- 에포크 : 전체 학습 데이터를 한번 싹 돌았다는것
- 1회독까지는 순서대로 해도 ㄱㅊ 2회독은 모르겠는 부분 부족한 부분 위주로 봐야지 빠삭한 부분을 할 필요 없음
  - ai도 마찬가지 굳이 아는 부분을 볼 필요가 봐봤자 과적합임!!
  - 즉 일반화 성능을 높이려면 계속 같은 순서대로 학습하기 보다는 순서를 다르게 해줘야 함

#### pretrained model
- 앞으로 모델을 볼때는 시스템의 복잡성(구조, 파이프라인)을 보는게 나음

### 모델 튜닝 및 최적화
#### transfer learning
- cutoff : 특정 기간 이전의 데이터들로 학습한것
- 파인 튜닝: 사전 훈련된 모델 일부 계층의 파라미터를 바꾸는것 (전체는 잘 안함, 아예 안한다는건 아님)
- feature extraction : 앞쪽, feature extraction만 가져와 사용
- zero-shot(few-shot) : shot은 샘플이므로 제로샷은 예시가 없다는것, 즉 한번도 가르친적이 없는걸 시키는것
  - zero-shot ex) 내가 갖고 있는 문서를 학습한 적 없는 llm에게 내 문서에 대해 질문하는 경우 
  - few-shot : 사례를 좀 들어주는것
  - 모델 자체의 파라미터를 바꾸는 방식은 아님 파라미터는 그대로 둠

#### FINE TUNING
- 용어를 잘 해주었으면
- fine : 잘잘하다, 세세하다
- fine - grained : 곱게 간것
- 풀파인튜닝 : 모든 파라미터를 재 학습 하는거 
  - 근데 가격이 너 ㅁ만이 들고 비효율적
- prtial fine tuning : 일부 파라미터만 재학습 하는것, 효율적
- 이외에도 여러가지 방법을 섞어서 사용함

#### 과적합
- 드롭아웃 : 과적합 방지 방법
  - 무작위로 뉴런 끄기
- 데이터 증강, 정규화 등 사용
- 너무 지나차게 학습 데이터에 맞추어서 문제

#### 과소적합
- 학습 데이터의 본질 적인 패턴을 학습해야 하는데 못한 케이스
- 아예 구조적인 문제가 있는 경우, 구조를 아예 개선해야 할 수 도 있음

#### Early Stopping
- 한 에포크마다 손실이 개선됨
- 원하는 기울기가 나오지 않는 경우 early stopping을 함
- 손실이 감소하지 않거나 증가하지 않으면 정지

#### hyper parameter
- 파라미터 == 가중치
 1. 피드 포워드 
 2. 손실 함수 값 
 3. 역전파 그레디언트(학습 방향)
 4. 최적화:매개변수 업데이터(학습 속도)

sgd, adam 같은거 안쓰고 내가 독자적으로 만들었다?? 포트폴리오 걍 미침;;
활성화 함수 독자적 대박;;;
- 초기값 설정에 신경쓰지 않은 이유
  - 랜덤이 나름 작동을 함
  - 좋은 초기값을 사용하는 장점 : 학습 속도가 빨라짐
- brute force = complete search = world simulation
- 정규화 파라미터 
  - 정규화 손실(regularized loss) : 모델이 지나치게 커지고 복잡한 것을 원하지 않음

#### 언어 모델
- 앞에 n-1개 단어가 주어졌을 때 다음 단어가 나타날 확률을 빈도 기반으로 계산하는 모델
- n-gram : n개의 단어 묶음, n-1개의 단어를 기준으로 다음 단어의 조건부 확률을 계산하는 전통적 통계 기반 모델, 즉 slm (지금도 많이 쓰임)
- 통계 : 기존 자료의 모음
- 6 morphological analysis
  - 형태소 분석
  - 단점 : 새로운 언어는 할 수가 업음
- 규칙 기반 -> 통계 기반 : 모델이 꽤 타당하게 돌아감

#### nplm
- nnlm(neural network)
- nn(neural netwrok)는 어떻게 이렇게 잘 함? 왤케 능력이 좋음?


#### rnn
- hidden state : 

### bptt
- 기본적인 cnn은 앞에 층으로 전달하면 되는거였음
  - 물리적으로 층이 구분되어 있음
- rnn은 층이 하나이며 이것이 반복해서 사용됨
  - 이전 층으로 전파

#### lstm

#### weekly challenge
- 현 사용하고 있는건  3세대 모델
- 1,2,3,4 번은 저번주까지의 복습
- 5번 화이팅!!!!
- 챗봇은 기본에 agent 까지 (개인프로젝트)
  - 외부꺼 가져다 써도 됨
  - 맥에서 실행되는 정도도 ㄱㅊ
  - gpu 활용해서 그럴듯한 성능 뽑고 싶다 -> 코랩
    - 코랩 사용시 fastapi로 감싸려고 너무 애쓰지 않아도 됨 (그래도 최대한 해줫으면 좋겠음)


#### 한줄 요약
- 모델 구성 : 딥러닝에서 모델은 가중치들의 집합을 의미하며 모델 구성은 다양한 레이어가 어떻게 결합하여 데이터를 처리하고 결과를 도출하는지 정의하는 모델의 구조입니다
- 사전 훈련 모델 : 이미 훈련이 끝난 모델 또는 구성요소를 말하며 처음부터 구축하지 않아도 되어 시간과 자원을 절약할 수 있기에 사용됩니다
- res net : 입력과 출력의 차이(잔차)를 학습하는 구조를 통하여 깊은 신경망의 학습 문제를 해결하고 성능을 향상시키는 모델로 이전층의 정보를 계속해서 받아온다는 특징이 있습니다
- vgg16 : 16개의 가중치 층으로 구성된 cnn을 말하며 1개의 입력층 13개의 컨볼루션 레이어, 3개의 fcnn으로 이루어져 있습니다
- finetuning : 사전 훈련된 모델을 새로운 데이터 셋에 맞춰 재 학습 시킴으로써 성능을 최적화 하는 과정을 말합니다.
- 특징 추출 : 사전 훈련된 모델의 중간계층을 고정하고 해당 계층을 특징 추출기로 활용하는 학습 방식을 말합니다.
- 제로샷 학습 : 학습된적 없는 데이터를 넣어 사전 지식이나 관련 정보가 없어도 이미 알고 있는 개념과 연관지어 인식하거나 추론할 수 있도록 하는 학습 방식을 말합니다.
- 과적합 : 학습 데이터를 과하게 학습하는것을 말합니다
- 과소적합 : 학습 데이터를 충분히 학습하지 못해 훈련 데이터와 새로운 데이터에서 모두 낮은 성능을 보이는것을 말합니다
- 조기 중단 : 모델의 성능이 개선되지 않는다 판단할때 학습을 조기 종료 하는것을 말합니다. 주로 과적합 방지 차원에서 사용합니다.
- hyperparameter tuning: 모델의 성능을 최적화 하기 위하여 모델의 파라미터 값을 조정하는 과정을 말합니다
- nlp : 인간의 언어를 컴퓨터와 같은 기계를 이용하여 묘사할 수 있도록 연구하고 이를 구현하는 분야를 말합니다.
- rnn : 순차적인 데이터 처리를 위하여 설계된 인공 신경망입니다
- lstm : 게이트를 통해 잊을 데이터 남길 데이터를 선택함으로써 출력과 멀리 있는 정보를 기억 할 수 없다는 단점을 보완해 낸 신경망 구조 입니다.


#### today review
- 다루지 않은 nlp 주제들
  - 우리의 공부 방식은 탑다운이므로 필요할때 와서 볼거임
- 화: rnn, lstm/트랜스포머 소개
- 수: 트랜스포머 리뷰/어탠션
- 목: 어탠션 리뷰/이번주 전체 리뷰
- 과제(위클리 챌린지), 개인 프로젝트 회고(포트폴리오 준비) 자세하게 쓰기
- 회고 쓰는 방법 : 내일 잘 쓴 회고 보여드림
- 트랜스포머 아닌 모델을 쓰나요
  - 당여나죠 트랜스포머가 만능은 아님 심지어 돈도 많이 들어