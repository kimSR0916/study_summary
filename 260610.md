### transformer
- rnn : 시퀀셜, 1차원
- cnn: 2차원, 근방
- transformer : 전체(들여다보는 범위)
- fcnn,cnn,rnn 은 기초모델 / 트랜스포머는 가장 최신 모델 (state of art model)
  - 둘 중 하나라도 빠삭하게 알면 전이학습이 가능해짐
  - 솔직히 트랜스포머 빠삭하게 공부하는거 ㅊㅊ 왜냐면 트랜스포머에 다 들어가있음
  - 이번주에 둘 다 못했다 하더라도 끝까지 하셈 Iteratively 하세요 모르겠으면 반복해서 하세요
#### input pipeline
- pipeline : 한과정의 프로세스가 이어져 있다
- 토크나이징(파이프, 단계, 프로세스) 
  - 자연어 문장이 들어오면 토크나이징(파이프)라는 단계를 통해 아웃풋을 만들어냄
  - 이 아웃풋으로 임베딩 -> 포지셔널 임베딩이라는 단계를 거침 (과정상의 인풋과 아웃풋을 알고있기)
  - 자연어 문장 -> 토큰화 : 토큰
  - 토큰 -> 임베딩 : 벡터(수)
  - 벡터 -> 포지셔널 임베딩 : 포지션 벡터(수) (하지만 임베딩 과정에서 나온 아웃풋과는 다른 벡터이다)
- 토큰 : 모델이 구분하는 입력 단위 (모델이 생각하는 자연어의 기본 단위)
- 토크나이저 : 텍스트를 토큰으로 나누는 전처리 모듈 (원천 데이터를 한번도 가공(전처리)없이 넣은 적 없었음 그 어느 과정에서도)
  - 규칙 : 핵심, 어떻게 쪼갤것인가
  - 사전 : 단어:수, 
  - 서브워드가 성능이 좋다는것이 발견 (ex. bpe 등)
  - 접두사 # : 다른 단어와 연결되어 있는것
- 토큰 임베딩 : 토큰이라는 수의 배열로 바뀜, 의미를 가지는 최소단위를 벡터 공간 (저차원)에 매핑하는것
- 패딩 : 여백 만들기, 보통 길이를 맞추거나 가리고 싶을때 사용
- 포지셔널 인코딩 : 모델이 토큰 순서를 구분 할 수 있게 해주는 기법
  - transformer는 순환구조가 없다 (재귀가 아님, 즉 순서대로 작업하지 않음, 순서라는 개념 자체가 없음)
  - 순서를 위치(좌표)로 바라보는것 (이를 통해 순환구조가 없는것을 극복)
- 인코더 
  - encoder -> context vector
  - 인코더 한 레이어는 두개의 서브레이어로 나뉘어져 있음
  - 뒷부분은 걍 ffn 앞부분은 multi-head self-attention
    - 어텐션이 한개가 아님
  - 번역 task는 인코더가 붙음
  - 인코더 온리 모델 : 감정 분류 같은곳에서 사용
- 디코더
  - 인코더의 출력과 이전까지 생성된 토큰을 참조하여 다음 토큰을 생성(예측) 즉 언어 모델
  - 우리가 쓰는 LLM 모델은 다 디코더 온리 모델

#### attention
- query,key, value 라는 세가지 벡터로 처리
  - query : 토큰이 찾고 싶은 정보
  - key : 각 토큰이 가진 검색 기준
  - value : 실제로 가져올 정보
    - 각 단어들끼리 얼마나 관련이 있나 , 최종적으로 어떤 정보를 줘야 하는가


#### 학습대상
- fcnn : 가중치 + 편향
- cnn : 합성곱:필터(가중치)
- nplm: 단어 임베딩 가중치 + fcnn
- rnn : 숨김 상태에 대한 가중치, 입력 가중치, 출력 가중치, 편향
- 트랜스포머
  - 토큰 임베딩
  - 포지셔널 임베딩
  - Q,K,V 가중치 행렬
  - FF(FCNN) 가중치
  - L(FCNN) 가중치
- [개념설명](https://outcomeschool.com/blog/math-behind-attention-qkv)

#### 1:N 멘토링 질문
- 신인에게 기대하는것
  - api 활용 시스템 구축이나 파인튜닝
  - api 활용은 시간이 많이 안들어가느 그렇다 치고 업계에 통용되는 능력은 파인튜닝이 더 좋은듯
  - 구현하는 능력은 수요가 그렇게 많지 않은듯
  - 원하는 전문성 어필 분야에 가중치를 알아서 두면 될듯
- 신인 당시 공부 태도
  - 뭘 물어봤을때 모른다고 대답하는건 프로의 자세가 아님 (확인해보겠습니다 알아보겠습니다)
  - 빠르게 포기하지 말고 좀 더 해봐라
- 요즘은 백엔드 ai 엔지니어
  - 가장 관심 갖는건 파이프라인
  - 단계가 세분화 되어있는 시스템 구현 필요
  - 백엔드는 devops 좀 해야함
- 자소서에 본인의 프로젝트 쓰면 좋을것
  - 각 개별 요소가 합불에 지대한 영향을 주는 경우는 ㅂㄹ 없는듯
  - 보통 종합해서 합불 줌
  - 그럼에도 좋게 보는게 있냐하면 근성을 좋게 봄
  - 카테부에서 가장 살아있는 스토리텔링을 만들어내는것이 좋음
- 흥미를 드러내는 요소
  - 그냥 억지로 지어내려고 하지말고 정말로 관련있는 회사와 지원하는게 괜찮을수도 있음
  - 억지로 만들어내려고는 ㄴㄴ 면접에서 들통날 수 있음 자연스러운게 베스트
- ai 사용 제한
  - 토큰 비용이 너무 비쌈
  - 성장을 하는데 가격이 계속 오르고만 있음
  - 오래 갈 현상은 아니고 조만간 저렴해지지 않을까
    - 로컬 ai 성능이 오라가지 않을까
    - 인프라가 확충이 되어서
    - 로컬 ai 시장이 더 커지지 않을까 싶음
    - 내부 ai 자체제작 ai 수요도 커지지 않을까?? 어쨏든 ai 투자가 많을것

### today review
- q : 쿼리 행렬(벡터의 모임)
- k : 키 행렬
- v : value 행렬
- 벡터 앞에 함수가 적용되면 엔트리 별로 그 함수 적용