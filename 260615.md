#### RAG
- rag(retrieval(=read, = search, 멀리 있는걸 가져오다) - augmented(증감됐다) generation)
- 고정확도의 유사성 기반 검색 기술
- 정확성과 최신성이 향상됨 (컷오프가 없다)
  - 가령 제미나이 컷오프가 25년12월 인데 26년 3월을 물어봐도 답 할 수 있는 이유는 웹 검색(rag) 때문이다
- 학습 데이터 이상의 정보 활용이 가능
- 방법
  - 검색의 대상을 확보하는것이 우엇ㄴ (문서수집 및 데이터 인덱싱)
  - 검색했으면 저장을 하면 됭 (인덱싱 및 저장)
  - 쿼리 및 검색
- 전처리와 청킹을 함
  - 들어가야 하는 텍스트 파일 사이즈에 한계가 있기에 데이터를 쪼개주는것(청킹)
  - 청크를 하게 되면 문서보다 청크 개수가 많다!! (문서를 갈아내면 청크가 만들어지는것이므로)
- 벡터디비와 rdb의 차이점 : 의미관계

- tokenization
  - 문장을 토큰으로 쪼개는것
  - 임베딩 단계에 해당
  - 트랜스포머와 똑같은 개념
- dense vector / sparse vector
  - 원핫 인코딩 (가장 쌩으로 넣은것, 고차원)
  - 1만어 어휘 사전이 있다 가정했을때 한 단어를 넣으려면 벡터 차원 1만차원 필요
  - 원핫은 남비가 너무 심하다!! 밀집하자 -> NLPM(embedding의 개념 적용 시작)
  - word data -> one hot encoding 1만차원 -> 프로젝션 (선형변환 행렬 가중치) -> 300차원 데이터 학습
  - dense vector : 모든 차원에 0이 아닌 값이 채워져 있는 상태
    - 의미(문맥) 관점, 저차원의 의미 공간적 매핑
  - sparse vector : 대부분의 차원이 0인 상태
    - 정확힌 검색, 키워드 검색용
- sentence transformer
  - 인코더계열

- vector db
  - 청크 인코딩(청크 벡터)은 저장한것이 아님
  - 즉 어딘가에 따로 보관하지는 않았던것
  - 벡터 저장과 벡터 검색을 하는 곳
  - rdb 테이블 -> 문법 기반 검색
    - (자연어) 의미 기반 검색 -> 번역을 주로 개발자(사람)이 해왔음
    - 자연어를 시퀄화 하는것을 사람(DB admin)이 했다는것
  - native semantic DB : vector DB
    - 어드민이고 나발이고 필요없다!!
    - rag 시스템의 핵심 저장소
      - 컴퓨터 메모리 보다는 우리 뇌 마냥 작동함
    - 사용자는 retrieval을 하고 개발자는 계속 채워놔야 함 (사용자와 개발군이 사용하는게 다름)
    - HNSW
      - 풀스캔 XX 대표를 조금 뽑아서 레이어를 낮춘 뒤 중간급, 최종급으로 레이어를 나눠서 관리
      - 대표를 뽑는 알고리즘이 따로 있음
  - chromaDB : 원문하고 함께 저장(프롬프트용)
  - 데이터가 모여있는곳을 컬렉션이라고 함
- faiss
  - 요즘은 걍 안씀
  - 거의 최초에 나온 벡터db (존재만 ㅇㅈ 해줍시다)
- 청킹
  - 문서를 쪼개는 작업(임베딩 목적)
  - 구분자 기반 청킹 (regex & delimiter chunking)
    - 정규표현식과 구분자 청킹
    - ex. \n\n(빈줄) / \n(줄바꿈) / #,##,###(마크다운 제목) / r'제\\d+조'(정규표현식)
  - 부모 자식 청킹
    - 의외로 뭉치는거에도 관심이 많음
    - 위계를 만들어서 관리하자
    - chunk를 문장화 할때 부모 청크를 넣는것
    - 맥락 자체를 풍부하게 만들어주는 효과가 있음
- 문서 로딩
  - 전처리는 에이전트 개발에서 비중이 큼
  - 상당히 까다로움!!
  - 특히 pdf가
- rag indexing
- Retrieval
  - RDB : 표 구조
  - GDB : 그래프 구조
    - Neo4j, apache jena(온톨로지까지 함) 가 있음

- context window
  - 맥락 = 사용자 입력(prompt) + 알파(좁은 의미의 맥락)

- prompt engineering


#### 한줄정리
- RAG : 정확도가 높은 유사성 기반 검색 기술로 ai들의 컷오프로 인한 한계 극복에 도움을 줍니다
- tokenization : 텍스트를 모델이 처리할 수 있는 토큰 단위로 분리하는 과정을 말합니다. 데이터 전처리 과정의 일종입니다.
- text embedding : 텍스트 의미를 보존한 채 고정길이를 벡터화 하여 유사도를 계산할 수 있도록 해주는 기법을 말합니다
- Dense Vector / Sparse Vector
  - Dense Vector : 모든 차원에 0이 아닌 값이 채워져 있는 상태로 문맥을 기반으로 검색한다는 특징이 있습니다
  - sparse vector : 대부분의 차원이 0인 상태로 키워드를 이용한 정확한 검색이 요구될때 주로 사용합니다
- sentence transformer : transformer 기반 모델을 사용하여 문장 전체를 dense vector화 하는 라이브러리 입니다
- vector database : 고차원 벡터 데이터를 저장하고 유사도 기반의 ANN 검색을 수행하는 데이터베이스 입니다
- hnsw : 벡터데이터를 계층화 해 서치하고 관리하는 인덱스 알고리즘 입니다
- chroma db : 임베딩 벡터와 메타데이터를 함께 저장하고 유사도 기반 검색을 제공하는 데이터베이스 입니다.
- FAISS : meta가 개발한 벡터 검색 라이브러리로 최초임에 의미가 있는 라이브러리 입니다
- 청킹 : 문서를 검색과 임베딩에 적합한 크기의 텍스트 조각으로 분할하는 과정을 의미합니다
- 구분자 기반 청킹 : 줄바꿈, 마침표, 제목 기호등과 같은 구분자를 기준으로 텍스트를 분할하는 청킹 방식 입니다
- 의미 기반 청킹 : 의미가 크게 변하는 지점에서 텍스트를 분할하는 청킹 방식입니다
- 부모-자식 청킹 : 청크를 계층적으로 관리하여 검색시에는 작은 단위로 llm에는 넓은 문맥을 전달하는 청킹 방식입니다
- 문서 로딩 : 다양한 형식의 원본 문서에서 텍스트를 추출하여 RAG에 투입할 수 있는 형태로 변환하는 과정을 말합니다
- pdf 파싱 : pdf 파일 내부 구조를 해석하여 텍스트와 메타데이터 등을 추출하는 과정을 말합니다
- 텍스트 전처리 : 문서에서 추출한 텍스트를 청킹이나 임베딩 전 노이즈를 제거하고 관리하는 과정입니다
- rag indexing : 외부 데이터를 검색하여 llm 에 입력 한 뒤 답변을 생성하는 구조의 전체 과정을 말합니다
- Retrieval (검색) : 사용자 질문을 벡터화하고 벡터 db에서 유사한 문서 청크를 검색하여 반환하는 단계를 말합니다
- generation : 검색된 문서를 기반으로 llm이 답변을 생성하는 최종 단계를 말합니다
- context window : llm이 한번의 요청에서 처리할 수 있는 최대 토큰 수 를 말하여 시스템 프롬프트, 검색된 문서, 사용자 질문, 생성될 답변을 모두 포함합니다.
- prompt engineering : 문서를 근거로만 답변하도록 지시하고, 환각을 줄이며, 출처를 명시하도록 유도하는 설계 기법을 말합니다

#### today review
- 어려운거 있으면 토로해라
- 화,수,목 1교시 복습 2교시 강의 식으로 나갈듯
- 허깅페이스(hugging face)
  - 입문, 계정, 토큰