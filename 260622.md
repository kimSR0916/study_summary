### 톺아보기
- 바닐라(프레임웍이나 라이브러리가 없는 기본) RAG
  - ex. 바닐라 js -> 리액, 앵글러, JQuery도 안씀 ㄹㅇ 쌩 기본 상태
  - 자바는 스프링 없이 뭘 만들지 못함 
  - 라이브러리, 패키지, 프레임워크는 절대적으로 필요한것이 아님
  - 그렇기에 프레임워크를 사용할때 이미 다 했는데 혹은 왜 이렇게 복자한가 하는 느낌을 받을 수 있음
- 면접 시 잘못얘기하면 면접관의 눈이 흔들림

- 강의가 끝나면 질문에 답해볼것
  - 7주차 : 랭체인 RAG
    - 왜 랭체인 없이도 할 수 있는데 써야하지?
      - 반복적으로 매번 똑같은 일을 하기에 이 반복을 줄여주는 역할을 함

  - 8주차 : 랭그래프 RAG
    - 왜 랭그래프 없이도 할 수 있는데 써야하지?
- 9주차 : 모델 튜닝
- 10,11 : 모델 서빙 준비
- 12 : 모델 서빙
- 13 : 개인 프로젝트 마무리 (스터디 조 해산)

- 이제는 프롬프트 엔지니어링이 필요 없다, ai가 스스로 프롬프트를 개선한다
- 자연어 넣고 자연어 추력이 어케 가능한건데
  - 구조화 출력을 처음부터 지원한것은 아니었음

(대회 용 TF조 생성)
- 14 : 부하테스트 대회
- 15 : 해커톤

- 현재 개인프로젝트(챗봇 + RAG) 아직 못만든 경우
  - 자기 모델 개발 : 선택
    - 안하면 API LLM 쓰면 됨
  - 바닐라 RAG 못했다면 랭체인 RAG으로 시작
  - 지나간 버스는 걍 냅두셈

- [ai시대의전문성](https://drive.google.com/file/d/19rY4idXdBoFyqqzu0ImZe45C31ETb7rC/view)

- ai 시대 시니어 개발자가 버려야할 것
  - 코드 작성량이 가치라는 믿음
  - 직접 구현하지 않으면 이해하지 못한다는 믿음
    - 시니어가 주니어에게 이렇게 강요하는것 아닐까
      - ex. 스프링, 랭체인
  - 코드가 생기면 조직 지식도 함께 생긴다는 믿음

### langchain
#### langchain
- 프레임워크 (뭔가 하는게 많음)
- llm 어플리케이션 프레임워크라고 불렀었으나 요즘은 그렇게 부르지는 않음 (요새는 agentic framework 라고 표현)
- 에이전트 만들때 사용
- component(부품, 부속품)
- interface
  - 기본적으로 lm(언어모델)은 파이썬 으로 다룸
  - 즉 lm의 가장 원초적인 인터페이스는 파이썬 언어이다
  - 파이썬 - CLI - WebGUI/App GUI 순
- 파이프라인 : 파이프(컴포넌트)가 순서대로 연결(인터페이스)
- 우리가 뭘 만들어보지도, 해보지도 않고 이해할 수 있는 이유가 뭘까
- 애초에 너무 지나치게 경험을 중시하는것 아닐까
- 상상력으로 부족한 경험을 채울 수 있다고 생각함
- LCEL 파이프라인 : 프롬프트 + 모델 + 파싱
- 회사 간다고 해서 langchain을 꼭 써야하는건 아님
  - langchain을 써야하는 이유 : 로직은 동일한데 모델을 왔다갔다 하고 싶을때 사용하면 편함
  - 메시지를 작성하는 과정을 간편하게 할 수 있다

#### messages
- chat model의 입출력을 구성하는 메시지 객체의 조합
- chat llm을 구조화 한것

#### LCEL
- LangChain Expression Language
- 파이프 연산자(|)를 이용하여 Runnable 컴포넌트 제작
- ex. chain = prompt|model|parser
- 주요 컴포넌트들이 모두 runnable interface

### 프롬프트 구성
- prompt : llm에 전달하는 입력 텍스트
- chatprompt template : 역할별 메시지 리스트 구성, 역할별로 구분
- fewshotprompt template
  - shot == 예시 1개
  - 수소의 입출력 예시를 프롬프트에 자동 삽입하여 원하는 형식으로 대답하도록 유도
- message placeholder
  - placeholder : 자리를 잡는것, 비어있는데 무언가 들어와 있는것

### 출력 구조화
#### structured output
- 스키마 == 구조체
- 회사 회의에서 가장 자주 사용되는 용어
  - 50%는 프롬프트 작성
  - 50%는 응답 파싱(구조화)

#### json output parser
- jasonputputparser : 프롬프트에서 양식 지시 + 출력 하여 구조 맞추기
  - 사유 : with_structured_output이 모델단위이니 훨씬 안정적이나 지원 안하는 모델들이 존재
- 파싱시 텍스트가 json 객체가 리턴

#### pydantic ouput parser
- pydantic
  - fastapi 에서는 요청 응답 스키마로 사용
  - langchain 에서는 응답 스키마로 사용

### runnable composition
#### runnable interface
- 텍스트 입력(단일) - 텍스트 출력(스트리밍) -> http, sse
- 그렇다면 입력도 스트리밍 출력도 스트리밍인 케이스가 있는가?
  - 텍스트는 없으나 보이스의 경우 그러함 -> WebRTC or WebSocket or gRPC 사용

#### runnable passthrough
- 기존 데이터는 넘기면서 추가를 할 수 있는 형태

#### runnable parallel
- parallel : 여러개
- 병렬로 뭐가 하고싶을때 사용
- ex. upper를 해주면 original과 upper 두가지 결과물이 동시에 나옴
- setup을 태워서 document의 context 값으로 넣어서 만들어낸것

#### runnable branch                                                    
- branch
  - 변수-연산-반복-조건(branch,condition)
  - 프로그래머의 큰 역할 중 하나가 분기 처리(상상력)
-입력 조건에 따라 서로 다른 runnable 실행

### 대화 이력 처리
#### runnable with message history
- 멀티 유저 시스템, 여러 유저가 동시에 씀
  - ex.A가 사용을 시작했다 = 서비스를 쓰기 시작했다 = 세션이 시작했다(Session_A)
  - 개인프로젝트는 기본적으로 멀티유저여야 함

#### message tuning
- 최근 몇개를 하는 방식도 있고 토큰 개수를 가지고 하는 방식도 있음


#### document
- 코드의 가동 범위는 변화 수용의 폭
- 프레임워크가 오히려 사람의 생각을 가둬두는 역할을 할 수 있음
- 팀 프로젝트 하면서 긁히는 일 진짜 많을것
- 소개 문서를 document 객체로 만드는것

#### document loader
- 외부 데이터를 document 객체 리스트로 반환 

#### text loader
- lazy_load : 로딩이 필요한 시점에 로딩 (즉각적인 로딩 xx), 미리 로더를 만들어놓고 로드 해야하는 시점에 호출

#### PyPDF loader
- 배운게 비슷해

#### WebBaseLoader
- url을 받아 html을 가져온뒤 beautiful soup으로 파싱하여 본문 텍스트를 document 객체로 변환하는 document loader 구현체
- 만약 웹에서 데이터의 일부를 가져오는걸 웹 스크래핑이라고 함
- 웹 스크래핑 업무가 없던적이 없음
- 만약 무신사에서 상품 사이트 스크래핑해서 상품 정보 다 긁어와 하면 결과물로 RDB가 나옴
- 테이블이 바뀌면 스크래핑 로직이 깨지는데 이 경우 로직을 다시 짜시면 됨^^
- 통 이미지 사이트 스크래핑은 OCR 호출 해야한다

#### directory loader
- *를 넣는 경우는 와일드 카드(임의의 이름이 들어갈 수 있음)
- 업로드시 맥에서는 커맨드, 윈도우에서는 컨트롤키와 함께 선택하면 다중 선택이 됨
- 디렉토리를 만든 뒤에 파일을 자동으로 분류를 함


### 위클리 챌린지
- RAG 파이프라인을 langchain으로 마이그레이션
- 마이그레이션한 langchain 기반 rag 파이프라인을 fastapi로 래핑하여 rest api로 배포
- langsmith로 체인 실행을 tracing 하고 dataset 기반으로 평가


### 한줄요약
- Langchain : LLM 애플리케이션 개발을 위해 표준화된 인터페이스로 제공하는 오픈소스 프레임워크
- Messages : Chat Model의 입력과 출력을 구성하는 역할별 메시지 객체의 집합
- LCEL : 파이프 연산자로 runnable 컴포넌트를 연결하여 체인을 구성하는 LangChain의 핵심 문법 체계를 말함
- prompt template : 프롬프트를 매번 새로 문자열로 만들지 않고 객체로 관리하여 값만 바꿔서 반복 사용할 수 있게 해 주는 도구
- ChatPromptTemplate : 역할별 메시지 리스트를 구성하는 프롬프트 템플릿 클래스
- FewShotPromptTemplate : "이렇게 해라"를 설명하기보다 "이런 식으로 해라"를 보여줄 때 사용되는 템플릿으로 소수의 입출력 예시를 프롬프트에 자동 삽입하는 방식이다
- MessagesPlaceholder : 실행 시점에 메시지 리스트가 삽입될 위치를 지정하는 컴포넌트 대화 히스토리를 프롬프트 안에 삽입하는 방식으로 사용됨
- Structured Output : LLM의 응답을 구조화된 객체로 반환하는 방식
- JsonOutputParser : LLM의 텍스트 응답을 JSON(Python 딕셔너리) 형태로 파싱하는 출력 파서 클래스
- PydanticOutputParser : LLM의 텍스트 응답을 Pydantic 모델 인스턴스로 파싱하여, 타입 검증과 필드 접근을 제공하는 출력 파서 클래스
- Runnable Interface : 서로 다른 객체를 같은 방식으로 실행하기 위한 표준 메서드 규칙
- RunnablePassthrough : LCEL 체인에서 입력 데이터를 변형 없이 다음 단계로 전달하거나, 새 키를 추가해야 할 경우 사용하는 Runnable 유틸리티 클래스
- RunnableParallel : 여러 Runnable을 동일한 입력으로 동시에 실행하고, 각 결과를 지정한 키에 매핑하여 하나의 딕셔너리로 합쳐 반환하는 Runnable 유틸리티 클래스
- RunnableBranch : 입력 조건에 따라 서로 다른 Runnable을 실행하도록 분기하는 Runnable 유틸리티 클래스
- RunnableWithMessageHistory : 이미 만들어진 LCEL 체인에 대화 이력 관리 기능을 덧붙이는 래퍼 클래스
- Message Trimming : LLM의 컨텍스트 토큰 한도를 초과하지 않도록, 정해진 기준에 맞춰 오래된 메시지를 제거하는 처리
- Document : LangChain에서 문서 한 조각을 표현하는 표준 데이터 객체
- Document Loader : 외부 데이터를 읽어 LangChain의 Document 객체 리스트로 반환하는 컴포넌트의 표준 인터페이스
- TextLoader : 단일 텍스트 파일을 읽어 한 개의 Document 객체로 변환하는 Document Loader 구현체
- PyPDFLoader : 파이썬으로 pdf파일을 읽고 페이지마다 한 개의 Document 객체로 변환하는 Document Loader 구현체
- WebBaseLoader : url을 받아 html을 가져온 뒤 본문 텍스트를 Document 객체로 변환하는 Document Loader 구현체
- DirectoryLoader : 지정된 디렉터리 안의 파일을 Document 리스트로 변환하는 Document Loader 구현체

### 리뷰
- 느린 코랩을 한페이지에 작성하여 해결 보자
- 위클리챌린지의 수치적인 평가
  - 모범 챌린지 공개로 대체 : 정량적인 평가는 고민 해보겠음
- 컴포넌트 관점 : prompt|model|parser
- 데이터 관점 : message|document
- 하이브리드 DB