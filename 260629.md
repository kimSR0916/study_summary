### 오프라인 운영
- 8:57 출석 호명
- 9:00 캡처 후 시작
- 월요일 한 줄 정리
  - 월요일 한 줄 정리 시간에만 워크스페이스 잡담 가능
  - 16:20 까지 개인별 정리 후 대화하는것
- 딥다이브 시간은 통일 (15:30 ~ 16:50 공유)
- 내일 일정
  - 오전 ot
  - 오후 13:00~15:00 수업
    - 15:00 ~ 17:00 1:N 멘토링 진행 (10,11,12,13)
- 후기는 여전히 줌 접속으로 동일
- 모레부터 스케줄 동일, 데일리 스크럼도 워크스페이스에서 진행 
  - 데일리 스크럼 9:00~9:30 그 이후는 자습
- 구글 코랩 cli 출시 [링크](https://developers.googleblog.com/introducing-the-google-colab-cli/)

### langGraph
- 랭귀지모델(ex. llm)의 언어에 가까움
- 랭귀지모델의 어플리케이션
- workflow(default) / chain
  - retriever - prompt - model - parser
  - 근본적으로 직선적이기에 단순한 면이 많음
  - 랭그래프의 방식을 모방하는 기업들이 늘어나고 있음
- 랭그래프
  - 무조건 그래프 구조로 설계하겠다!!
  - 원래는 그래프가 자료구조 이상의 의미가 없었으나 ai의 도입 이후 사용되기 시작
  - 그렇기에 ai를 다룸에 있어 그래프는 굉장히 중요한 기초
  - 한줄정리할때 개념이 속한 분류를 먼저 생각할것 (ex. 랭그래프는 에이전트(를 만드는데 사용되는) 프레임워크이다)
  - llm이 실행 순서를 정하는것이 아닌 개발자가 실행 흐름을 그래프로 미리 설계한다는것에 의미가 있음
  - 파이프라인 설계하라 : 업무의 흐름을 설계하라
  - 즉 랭그래프는 사실상 llm을 안내해주는 가이드 
  - 불안정한 llm을 안정화하고자 사용
  - typedict 굉장히 자주 씀 랭그래프도 랭체인도

### workflow(in LangGraph)
- 작입이 진행되는 흐름
- 워크플로우는 단계적으로 상태를 다루는 앱 구조다
- chain : 직선 구조 / graph : 그래프 구조 / workflow : 단계 순서 구조
- 소통할때 중요한건 결국 설명!! 물론 실력도 중요하지만 설명을 해야 소통이 된다
- 결국 현 역량의 우선순위는 실행력 >= 소통력 > 지식 순

### ai agent
- 에이전트는 도구 호출 등의 행동을 하게 하는 실행구조다
  - 한줄 요약할때 뭘 살릴지 왜 살리는지 고민하며 해보자
- 소프트웨어는 두뇌 에이전트는 손발
- 클로드 코드와 같은것은 코딩 에이전트라 호칭함
- 코딩 에이전트에 정말 많은 도구가 들어가 있음
  - 즉 님들이 코딩 에이전트를 직접 만들거면 다 추가해놔야함

### graph api
- 그래프를 구축하는 랭그래프의 기본 api 이다

### state graph (상태 그래프)
- 스냅샷 : 순간포착
- 즉 현재 시점의 정보를 담는 그래프
- 랭그래프가 실제로 실행되는 시점은 invoke 할때임
- 상태 그래프는 빌더(무언가를 생성해내는것, 여기선 그래프) 클래스이다

### graph state schema
- 그래프 상태 스키마는 상태 구조의 정의(definition)다
- 한줄 정리 방법 (앞으로 정리시 이 방식 추천)
  - 1단계 : A는 B다
  - 2단계 : A는 ...한 B다
  - 3단계 : A는 ...를 하려고 하는 ...를 가지고 하는 ...를 ...한 B다.
- 근본적으로는 ai를 잘쓰더라도 기본이 되는 인재를 선호하고 있음, 언제나 기본 능력을 갖추고 있자 (ai 없이도 개발을 할 수 있도록)
- 한줄정리는 실시간으로 머리에 떠오르는걸 정리하는 시간

### reducer
- 결합해서 줄이는것
- 여러개의 입력을 하나의 출력으로 만들어줌
- map reduce
  - n개의 데이터가 들어왔을때
  - map : 1 -> '1'
  - reduce : '1','2', '3' -> '123'
- 한줄 요약
  - 리듀서는 함수다
  - 리듀서는 합치는 로직을 담은 함수다
  - 리듀서는 기존 값과 새 값을 합치는 로직을 담은 함수다

### message state
- 한 줄 요역
  - 랭그래프의 상태 스티마다
  - 랭그래프의 기본 상태 스티마다
  - 메시지를 누적하는 랭그래프의 기본 상태 스티마

### node 
- 한 줄 요약
  - 노드는 함수(실행단위) 이다.
  - 노드는 상태를 입력 받아 갱신할 부분을 출력하는 함수다
  - 노드는 상태를 입력 받아 작업을 수행하고 갱신할 부분을 출력하는 함수다

### edge
- 한 줄 요역
  - edge는 그래프 연결이다
  - edge는 노드 실행을 표현하는 유향(directed)그래프 연결이다.
- literal : 둘중에 하나가 값으로 들어감

### start/end
- 그냥 시작점 끝점

### 컴파일
- 실행 위치

### 그래프 시각화(graph visualization)

### tool calling
- 랭그래프의 핵심
- 랭그래프를 쓰는 이유
- 한 줄 요약
  - tool calling 은 기능이다
  - tool calling 은 도구의 호출을 판단하는 기능이다
  - tool calling 은 사용자 입력 분석 결과로 도구의 호출을 판단하는 기능이다
- llm이 도구를 직접 실행하는것이 아닌 사용자의 요청을 분석하여 판단을 내림
- 즉 llm은 도구를 실행하는것이 아니라 판단을 통해 실행하도록 요청하는것
- llm이 계산을 못하는건 아니지만 굳이? 그냥 장점을 살려 쓰자 비효율적이다

### tool node
- 한 줄 요약
  - 툴 노드는 노드다
  - 툴 노드는 사전 구성(prebuilt)된 노드다
  - llm이 판단한 도구를 바탕으로 실제 수행하여 결과 메시지를 만드는 노드다

### 라우팅
- 라우터 : fastapi (매핑)
- 한 줄 요약
  - 라우팅은 디자인(설계) 패턴이다
  - 라우팅은 입력 또는 상태에 기반하여 다음 실행 노드를 선택하는 디자인(설계) 패턴이다
- 앞으로는 개별 기술보다는 논리가 중요하지 않을까
- 논리 : 수학, 물리

### parallelization(병렬화)
- 한 줄 요약
  - 병렬화는 패턴이다 
  - 병렬화는 병렬 실행을 위한 패턴이다
  - 병렬화는 하나의 노드로부터 여러 노드에 연결되어 동시에 실행된 후 모든 노드의 결과를 취합하는 병렬 실행 패턴이다 
- 제미나이를 쓰더라도 라우팅 속도를 높이는걸 고민해볼것
- 라우팅은 llm에서 정말 많이 쓰임
- 직렬로 처리하면 너무 느릴때 병렬 사용
- 긴 텍스트는 당연히 느릴것 이때 응답을 빠르게 하고 싶다면 분할처리해서 병렬화 처리 할것 (비용은 보면 안됨)

### send
- 한 줄 요약
  - send는 객체(object)다
  - send는 동적 분기 객체(object)다
  - send는 병렬 실행을 하는 동적 분기 객체(object)다
- 비용 말고 큰 단점이 없어 어지간하면 병렬처리를 하는것이 우세
- 단순히 문서 세개를 동시에 하는거면 문제가 없겠지만 긴 문서를 쪼개서 요약을 하면 정보 분절이 발생할 수 있음 (병렬을 무작정으로 해결하기에는 부족함)
- 오버랩도 효과가 없을때가 있음

### command
- 한 줄 요약
  - command는 객체다
  - command는 랭그래프 객체다
  - command는 상태 갱신과 다음 노드 지정을 한번에 할 수 있는 랭그래프 객체다
- 라우팅 함수는 조건부 edge에 등록
- 라우터 노드: command 객체를 반환하는 함수를 노드로 등록

--- 
### 지금부터 진짜 중요

---

### loop
- 순환 경로 == cyclic path
- 한 줄 요약
  - Loop은 구조다
  - Loop은 랭그래프 구조다
  - Loop은 종료조건이 만족될때까지 반복하는 랭그래프 구조다
- 즉 종료조건이 아예 없으면 (있는데 만족하지 못하면) 무한루프가 됨
- 무한루프가 무조건 나쁜건 아님
- 루프 돌다가 스스로 종료해버리는 문제(전기나 시간, 자원때문에) == 튜링
- 한줄정리 == 첫번째 복습
  - 정리할때 내가 이해가 가는 문장으로 할것 (그대로 적기 ㄴㄴ)
  - 그래야만 백지 테스트 통과 가능
- 개인 프로젝트에서 그래프 기능인거 죄다 랭그래프로 옮겨보셈
- 개인 프로젝트의 랭체인 기반을 랭그래프 기반으로 바꾼다
  - 이후 확장해서 ai agent로 발전 시키기 (기능적으로 rag 이상의 기능을 추가해보라는것(추가를 못해도 ㄱㅊ))

### loop termination
- loop의 종료조건
- loop를 안전하게 끝내는 종료 설계 조건
- 한 줄 요약
  - loop termination은 구조다
  - loop termination은 종료를 어떻게 할지에 대한 설계 구조다

### ReAct Pattern
- Reasoning(추론) + Act(행동) 패턴
- llm이 추론과 행동을 교대로 반복하며 문제를 해결하는 접근법
- 한 줄 요약
  - 리액 패턴은 구조다.
  - 리액 패턴은 에이전트의 실행 구조다.
  - 리액 패턴은 추론하고 실행한 결과를 바탕으로 반복하는 에이전트 실행 구조다.
- 어떤 일을 어떻게 하는지 자체를 에이전트가 하게 하는것이 목적 (판단과 행동을 둘 다 맡겨 일을 줄이는것)
  - 이를 AX AI transform 판단 기계

### functional api
- functional api는 함수 호출 흐름은  workflow로 실행하는 api다
- functional api는 약간 부가적인 느낌이고 큰 틀은 그래프 api 로 가는게 맞음(이런 기능이 있다 정도로만 알아둘것)

### persistence (연속성)
- 기능이 중간에 멈추거나, 한번에 대화가 여러개 들어와 동시에 여러 갈래로 대답해야 하는 상황에 처할때가 있음
- 대화가 사라지면 곤란해지는 상황이 생길때가 많아, 한번에 진행해야만 할때가 있음
- 프로젝트에서도 이 기능은 필수

### 한줄정리
- LangGraph : 랭그래프는 계속해서 갱신되는 데이터가 있는 llm의 세부동작을 제어할 수 있는 프레임워크다 
- workflow : 워크플로우는 특정 순서와 조건에 따라 단계적으로 처리되는 llm 애플리케이션 구조다 
- AI agent : ai agent는 llm이 현재 상태를 기반으로 다음 행동을 선택하는 실행 구조다
- graph api : 그래프 api는 상태그래프 클래스를 이용하여 그래프를 구축하는 랭그래프의 인터페이스이다 
- state graph : 상태 그래프는 상태 스키마를 기반으로 하여 그래프를 구축하는 생성용 클래스이다
- graph state schema : 상태 그래프 스키마는 상태의 규칙에 대해 정의한 상태 구조 정의이다
- reducer : 리듀서는 기존 값과 새로운 값을 합치는 방법을 정하는 함수이다
- message state : 상태 메시지는 대화 메시지 기록을 리듀서를 통해 관리하도록 제공되는 랭그래프 스키마이다

### 수업 리뷰
- 앞으로는 한줄정리 시간에 토픽도 그냥 종이에 적어놓고 안보고 발표할것
- 종이와 펜과 얼굴에 익숙해지자 (면접과 최대한 근접하게)
  - 딥다이브도 이렇게 하는거 추천
- 아무 도움 없이 설명할 수 있도록 노력해보자
- 회의실은 팀프로젝트 시작하면 예약제로 가동
- 오예 내일부터 카메라 끈다
- 기능을 익히기 위한 미니프로젝트 주제를 주겠음 
- 타운홀 뒷편 주무셔도 댑니다
- 본연의 능력을 키우는 방법
  - 비효율로 돌아가야 한다 