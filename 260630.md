### 강의 들어가기 전
- 비동기 소통 꼭!!! 할것
  - 대화 가능한 시간은 한 줄 정리 공유/ 데일리 스크럼/ 딥다이브 공유 시간밖에 없음
  - [시간표](https://cdn.discordapp.com/attachments/1521296275788791949/1521328257276969111/2026-06-30_10.33.39.png?ex=6a446ed9&is=6a431d59&hm=00f7794eb2d5eaa1e88c84051f5a80997e00ee62185b7fd775284187a38d34b4)
  - 6시 이후에도 대화 금물
  - 한 줄 정리 할때 박수 치지 마셈
  - 리액션도 자제 하셈 의미없는 추임새 하지 마셈!!
  - 쉬는시간에도 대화 금물
  - 전화 << 이건 진짜 미쳤니
- 7월에는 토큰(gpu사용량) 절약할것
  - 수업에 사용해야됨
- graph api vs functional api
  - graph api : 원조, 랭그래프 나왔을때부터 사용
    - 보일러 플레이트 (기능을 하기 위해 짜야하는) 코드가 너무 많음
    - 그래프라기 보다는 일직선에 가까움
  - functional api : 비교적 최근 도입 됨
  - 복잡한 흐름도 vs 간단한 워크플로우 + 랭그래프 고유 기능
    - 그래프는 복잡한 흐름도를 수용하는데에 적합함
- ReAct 미니 프로젝트
  - 필수 xx 도전용
  - 멀티 소스 교차 검증 자동화 팩트 체커
    - 웹 검색은 최신 뉴스나 블로그
    - 위키피디아는 객관적 정보
    - 성향 분석기 : 특정 언론사나 사이트의 성향
  - 이런 아이디어를 구현하면 좋을것 같당
  - 쇼착 상태에 빠졌을때 추가 검증을 위해 출처를 찾아 나서는것

### persistence(영속성)
- 헌 줄 요약
  - 영속성은 기능이다
  - 영속성은 상태를 유지하는 기눙이다.


### check pointer
- 그래프 실행 위에 특정 지점
- 한 줄 요약
  - 체크포인터는 인터페이스다
- InMemorySaver : 가장 간단한 체크포인터 구현체
  - 인 메모리 이므로 시스템 메모리이다 (즉, 휘발성임)
  - 키 : thread_id
  - 팀프로젝트에서는 postgresSaver(대화 내용 저장함) 쓸듯
  - 이외에도 sqliteSaver가 있음

### thread
- 꼬리 질문 받았을때 몰라서 답이 안나와 당황할때 포기하지 말것
  - 힌트 좀 달라고 하자 (ex. 혹시 그 주제에 대해 조금 더 맥락을 설명해 주실 수 있을까요)
  - 면접은 뻔뻔하고 당당하자
- 스레드 id 를 키로 받는다는건 유저 별로 스레드를 할당하겠다
- 멀티 유저 시스템을 위해선 스레드가 필수임
- 세션 실행 단위

### checkpoint
- 한 스레드 안의 상태 스냅샷 (체크포인트는 상태 기록이다)
- 체크 포인터가 저장하는 정보
- 특정 시점의 정보를 계속 쌓아올림(저장)
  - 이러한 특성 때문에 디버깅으로 보는 시선도 있음
  - 문제가 발견하면 해결을 위해선 문제를 재현해야 함
  - 즉 재현이 되는 문제면 오히려 이득임 재현이 안되는 경우가 진짜 큰일(해결을 못함)
  - nonreproducible한 버그는 고치지 않음 (못고침)

### timetravel 
- 체크포인트와 같은 이야기

### durable execution
- 장기 실행
- 원래 소프트웨어는 0.5초 이내에 답이 나오도록 했음
- 다만 지금은 llm이 무거워서 불가
- 원래도 ide 만 써서는 빠른시간내에 답을 주지 못했으나 캐시를 통해서 빠른 답장이 가능하도록 함
  - 즉 유저들이 요청하는것에 공통점이 많음
  - llm은 캐시를 달아도 효율이 없을 가능성이 높음
  - 결국 인공지능은 응답이 오래걸리는것에 익숙해져야 함 (속도 업이 많이 어렵다...)

### determinism
- 답이 애매하게 오지 않고 확실히 떨어짐
- 답이 일관성을 유지하도록 해줌

### idempotency
- 멱등성 : 단어 자체를 잘 알아야 함
- 부수 효과가 있는 서브루틴이 여러번 호출되어도 한번의 호출과 같은 시스템 상태에 대한 효과가 있다면 멱등적이라고 함
  - ex. 고객의 주소를 특정 주소로 수정하면, 여러번 호출해도 최종적으로 바뀐 주소는 동일
  - 즉 한번 호출을 했을때나 두번 이상 호출을 했을때나 결과가 똑같음
  - 그렇기에 한번 이상 호출을 할 필요가 없음
- 부수 효과(side effect)
- 서브 루틴 == 함수
  - 함수를 호출했을 때 발생하는 효과
    - main : 반환값
    - side : 반환값이 아닌 발생하는 모든 것

### today review
- 내일 멱등성과 부수 효과 보충
- 개인프로젝트 상담은 1:N 이후 와서 상담
- 팀프로젝트 시작일 : 8/24
- 개인프로젝트 마감일 : 8/7
- 8/10~8/12 : 부하 테스트
- 13.14 : 휴강
- 8/18~8/21 : 해커톤
  - 18일은 강의
- 밤샘 금지!! 
- ai 도 캐싱이 가능하긴 함
  - 프롬프트 캐싱
    - 키워드 기반 캐싱(검색) : 정확하게 동일한 프롬프트가 들어올때
    - 의미 기반 캐싱 : 질문을 임베딩 해놓는거임 그래놓고 질문과 유사한걸 찾아서 그 답을 출력 << 이거 상당히 효과가 있음, 대신 정확도가 떨어질수도 있음
  - 컨텍스트 캐싱 : RAG 수준에
    - 문서 검색에 캐싱 달기 (자주 조회되는 문서는 미리 넣어버리는거임)
- vllm 달기
- aws gpu 배포 해야함 ㅋㅋ ㅅㄱ해라