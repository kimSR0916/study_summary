### opening
- 스트럼
  - 어제 한 것, 오늘 한 것, blocker를 통해 개선의 방향을 잡고자 함
  - 15분 작성 / 15분 발표 나쁘지 않은것 같음
- 클로드 페이블5, 소넷5 출시

- 멱등성 idempotence
  - 눌렀을때마다 달라지면 멱등성이 아님
  - 어떤 연산의 성질
    - 여러번을 적용해도 결과에 변화가 없음
- 부수효과(부작용은 소프트웨어 업계에서 사용하지 않음) side effect
  - 함수의 주 효과 : 함수를 읽고 반환해주는것
  

### short term memory
- 대화 내용 저장

### long term memory
- 대화 내용을 요약하여 저장
- 추후 검색(RAG)의 형태로 대화 내용을 조회

### streaming
- fastapi와 연관이 좀 깊음
- 아무리 프런티어 모델이라 하더라도 답변이 오래 걸리는 경우가 생길 수 밖에 없음
  - 즉 완성했을때만 답을 주기엔 사용자 친화적이지 않음
  - 진행 도중 결과를 점진적으로 출력하도록 하는것이 스트리밍

### architecture
- 설명이 가능해야함 (정의, 방법, 이유)
  - 하고 싶은게 있으면 설명 할 수 있어야 함
#### interrupt
- ai 한테 모든걸 맡겨도 되겠는가
  - 사람이 승인하는 시나리오
- 인터럽트는 종료가 아님 정지
- 은근 프로그램에서 정지를 구현하는게 어려움
- human in the loop(HITL)
  - 인터럽트는 hitl의 상위 개념 인터럽트는 정지 자체를 의미하며 hitl은 사람이 개입한 형태

#### plan and execute patter
- 계획실행패턴
- 계획을 먼저 수립해서 순차적으로 실행하고 필요시 남은 계획을 수정하는 에이전트 패턴
- 계획 -> 실행 -> 결과를 평가하여 계획 수정 -> 실행 반복
- replan과 plan에는 차이가 있음
  - plan은 좀 전반적임, 전체적
  - replan은 실행되지 않은 부분 위주
  - 기능적으로 비슷하다고 하여 하나로 묶지 않는게 좋음

#### evaluate optimizer pattern
- 평가해서 기준에 안맞으면 재생성
- 일종의 loop

#### subgraph
- 그래프를 모아서 하면 안되는걸까
- 그래프를 노드로 하여 모듈화 단위로 만들어버린다
- 큰 그래프의 노드 역할을 하는 작은 그래프 즉 노드에 들어간 그래프를 말함

#### multi-agent system
- 지금까지는 agent 한개
- 여러 전문 agent가 업무를 분담하여 협업하도록 하는 시스템
- 님들은 구현을 하셔야해요
- 대부분의 작업(두뇌)이 llm 물론 인간이 개입을 해야하긴 함
- 현재 도입하고자 하는 시스템