### opening
- 스트럼
  - 어제 한 것, 오늘 한 것, blocker를 통해 개선의 방향을 잡고자 함
  - 15분 작성 / 15분 발표 나쁘지 않은것 같음
- 클로드 페이블5, 소넷5 출시

- 멱등성 idempotence
  - 눌렀을때마다 달라지면 멱등성이 아님
  - 어떤 연산의 성질
    - 여러번을 적용해도 결과에 변화가 없음
- 부수효과(부작용은 소프트웨어 업계에서 사용하지 않음) side effect
  - 함수의 주 효과 : 함수를 읽고 반환해주는것
  

### short term memory
- 대화 내용 저장

### long term memory
- 대화 내용을 요약하여 저장
- 추후 검색(RAG)의 형태로 대화 내용을 조회

### streaming
- fastapi와 연관이 좀 깊음
- 아무리 프런티어 모델이라 하더라도 답변이 오래 걸리는 경우가 생길 수 밖에 없음
  - 즉 완성했을때만 답을 주기엔 사용자 친화적이지 않음
  - 진행 도중 결과를 점진적으로 출력하도록 하는것이 스트리밍

### architecture
- 설명이 가능해야함 (정의, 방법, 이유)
  - 하고 싶은게 있으면 설명 할 수 있어야 함
#### interrupt
- ai 한테 모든걸 맡겨도 되겠는가
  - 사람이 승인하는 시나리오
- 인터럽트는 종료가 아님 정지
- 은근 프로그램에서 정지를 구현하는게 어려움
- human in the loop(HITL)
  - 인터럽트는 hitl의 상위 개념 인터럽트는 정지 자체를 의미하며 hitl은 사람이 개입한 형태

#### plan and execute patter
- 계획실행패턴
- 계획을 먼저 수립해서 순차적으로 실행하고 필요시 남은 계획을 수정하는 에이전트 패턴
- 계획 -> 실행 -> 결과를 평가하여 계획 수정 -> 실행 반복
- replan과 plan에는 차이가 있음
  - plan은 좀 전반적임, 전체적
  - replan은 실행되지 않은 부분 위주
  - 기능적으로 비슷하다고 하여 하나로 묶지 않는게 좋음

#### evaluate optimizer pattern
- 평가해서 기준에 안맞으면 재생성
- 일종의 loop

#### subgraph
- 그래프를 모아서 하면 안되는걸까
- 그래프를 노드로 하여 모듈화 단위로 만들어버린다
- 큰 그래프의 노드 역할을 하는 작은 그래프 즉 노드에 들어간 그래프를 말함

#### multi-agent system
- 지금까지는 agent 한개
- 여러 전문 agent가 업무를 분담하여 협업하도록 하는 시스템
- 님들은 구현을 하셔야해요
- 대부분의 작업(두뇌)이 llm 물론 인간이 개입을 해야하긴 함
- 현재 도입하고자 하는 시스템

### 1:N 멘토링
- 질문지의 경우 답변이 어려울 수 있음
- 모델 변경 시도시 수동 선택도 좋지만 api 한도 막혔을때 자동으로 다른 모델로 넘어가도록 설정해보는것도 좋음
  - 요즘 이런식으로 자주 함(fall back, 실패했을때의 대안, 서비스 무결성을 위함)
  - 다만 장기기억을 해야하는 경우 모델이 변경되면 좀 어려움이 있음
  - 임베딩을 로컬에 하거나 허깅페이스 모델을 쓰는 경우도 있음
    - 서버의 os 는 리눅스 cpu는 인텔계열일것 즉 gpu가 존재하지 않기 때문에 성능 저하가 생길 수 밖에 없음 (gpu 서버는 굉장히 비쌈)
    - 지금 모델을 aws 에서 돌리기란 어려울것 그래서 코랩 서버에 걍 띄우는 경우도 있음
    - 그렇기에 로컬 임베딩은 조금 어려울것이고 경쟁력을 찾기가 어려움
- 모델을 만들때 평가set을 만들어서 평가해보기 절대평가만 말고 다른 모델과의 상대비교도 해보셈
- 본인 모델의 특기를 찾아볼것

### review
- ai 가 공식 문서 기반으로 답변을 하게 가이드를 주고싶다
  - 아직은 ai 가 공식 문서 주소를 모를수도 있을것 같음
  - 그렇기에 좋은 개인프로젝트 소재라고 생각함
  - 공식문서가 없는 경우 위키피디아로 대체하는 등 fallback 도 대응 가능
  - 스킬로 만들어도 괜찮을것 같음
- checkpointer와 장기메모리/단기메모리 관계 내일 다시 복습
- 유튜브 비공개 스트리밍 << 한번 고려해보겠습니둥
- 내일부터 칠판 위치로 바꿀듯
- 디스코드 에티켓
  - 무조건 존댓말, 반말 금지
  - 대답은 무조건 스레드로 (최근 소통 하는 방식의 기본)
    - 회사에서도 이렇게 사용이 됨, 답장 기능도 사용 ㄴㄴ 무조건 스레드로 사용
- YAGNI
  - you aren't going to need it : 너는 그것을 필요로 하지 않을것이다
  - 미리 많이 짜지 마라
  - 필요하지 않은걸 괜히 지래짐작해서 짜놓지 말아라
  - [링크](https://news.hada.io/topic?id=30984)