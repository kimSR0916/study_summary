#### 일정
- 오늘 : 1~22
- 내일 : 23~29, 위클리 챌린지
- 모레 : unsloth, sft, rl
- 글피 : 발표 희망자 접수(희망자 디코 dm), 한명당 10분
- 이제 분량 3/1 밖에 안남앗다~~
- 무언가를 할때 제일 하지 말아야할것은 중도포기하는것
  - 일을 하면 끝까지 책임지자 회피하지 말자
- 내가 배워야 하는 기술이 ai가 학습이 안됏을 정도로 어려운 기술일 수 잇다
  - 하지만 도망치지 말자..! 회피하지말자.......!!!!!!!!!!!!!!!
- 관찰(observation)
  - 단순히 보는 것 이상으로 분석(잘게 쪼개어서 파악)을 해야함
  - 소프트웨어 업계는 관찰을 한다기 보다는 관찰 대상을 만들지 않나
- 물가는 죄다 오르지만 유일하게 떨어지는건 인건비밖에 없다 ㅜㅜ
- ai는 상상과 공감을 못함
  - ai 가 하는건 아부 아첨의 영역 (제미나이가 이게 진짜 심함)
- 이걸 개발자가 해야하는데 잘 못해서 보통 기획자에게 맡김
  - 즉 기획을 다 해서 정해서 주세요 라고 함
- 모델을 받으면 보통 하는게 최적화 작업과 서빙 작업임
- provider-agnostic(제공자 상관없이) 개발을 하는게 좋음.. 우리 교재는 그렇지 못해 아쉬운편 즉 제미나이 api 사용을 다 하면 대응 방법이 없는 것
- 심지어 프롬프트도 모델별로 적용되는게 다름 제미나이에서 성능 최대로 뽑아낸 프롬프트가 오픈ai에선 생각한 만큼 결과가 안나올수도(lock-in 효과)
  - 이거 대처하려면 프롬프트 작성할때도 multiple 모델로 테스트하세요(에르메스나 self agent 만드세요)

### llm optimization
- 알맞는 형태로 만드는 과정
- 학습이 끝난 llm을 개선하는 과정(학습이 끝난 모델은 너무 높은 수준의 하드웨어를 요구할 수 있음)
  - 양자화, peft, 배포형식
- 데이터타입/양자화 : 비트 수를 줄여 메모리 사용량과 추론시간을 낮추는 방법

### 데이터 타입
#### fp32
- 32비트 부동소수점
- 처음에는 고정 소수점을 통해 실수를 표현하였음
  - 자릿수가 고정되어 있으니 계산이 용이함 
  - but, 한계가 명확함 (표현 범위가 굉장히 좁아짐, 최대치, 최소치 표현가능수 전부 다 좁아짐)
- 결국 고정 소수점의 표현력의 한계를 극복하기 위하여 나온것이 부동 소수점
- IEEE754 가 업계 평균, 이걸 기준으로 만들어야 함
  - IEEE754가 최고 밑바닥, 더 깊이 들어갈 수 없음
  - 부동시스템 연산을 구현하고 싶은 사람이 봐야하는 문서
- posit
  - 부동소수점은 자리가 정해져있는데에 반해 (지수 몇자리/ 가수 몇자리) posit은 지수와 가수의 길이가 가변적으로 감
  - 괜찮은것 같은데 왜 안쓰지?? >> 이러한 수 체계는 하드웨어의 영역, 이미 많은 하드웨어들이 IEEE754에 맞춰져 있기에 새롭게 적용하기가 어려움, 보통 양자컴퓨터가 hpc 일부에서 사용되고 있음
#### fp16
- half precision(반정밀도)
- 범위는 줄이지는 않았는데 정밀도를 많이 줄인다
- 사용량이 적으므로 모든 면에서 효율적일 수 밖에 없음 (효율의 대가는 주로 정밀도가 감당)

#### bf16
- brain float 16
- google brain(구글의 ai 조직) 에서 나온것
- 지수 범위를 키움(fp32와 동일)
- 크기에서는 fp32에 밀릴건 없지만 숫자의 세밀함은 떨어짐

#### mixed precision training
- 일부는 16 일부는 32 같은 방식으로 섞어서 사용하는것
    ex. 가중치 업데이트 할때만 32 사용하고 나머지 과정에서는 16을 사용하는 방식

#### INT8
- fp32가 mm까지 표현된 자라면 INT8은 1cm 간격의 손바닥 자 
- 양자화 원리 : 실수를 넣으면 정수가 나오도록, fp32의 값을 원본 그래도 저장할 수는 없음 (손실을 감수해야만 함)
- 성능 감소는 감수해야만 함 (애초에 값을 보존하는 형태의 변환이 아님)

#### int4
- 요즘 하다하다 1비트 모델도 나오고 있음

#### 양자화
- quantization
- 더이상 쪼개지지 않는 단위
- 양자는 즉 최소단위이다
- 최소 양이라는 것 자체가 연속인 경우 정하기가 어렵다 
  - 그렇기에 실수는 어떤수를 잡아도 더 작은수를 잡는게 가능함
  - 그 반대로 실수는 어떤수를 잡아도 더 큰수를 잡는것도 가능함
  - 두 유리수 사이에는 반드시 유리수가 존재함
- 실수로 저장된 모델 파라미터를 정해진 규칙에 따라 낮은 비트수 표현으로 변환하는 경량화 비법 (fp32 -> int8)
- 즉 모델 양자는 모델을 묘사하는 최소의 양인것이다
- 가장 손쉬운 형태의 양자화는 가중치 한정이며 대부분 이걸 사용함 (주로 하게 될 케이스)

#### affine 양자화(변환)
- 어파인 변환 
- 선형변환에 평행이동이 가미된것
- y = ax + b 에서 b의 역할
- 모델 로딩이란 허깅 페이스로부터 정보를 내려받는것을 의미함 (FP16)
  - 모델의 가중치를 파일에서 비디오 메모리(gpu)로 올림 (INT4) -> 양자회 진행, 그렇기에 사이즈가 줄어들음

#### weight quantization
- 가중치 양자화
- 학습된 가중치를 저비트 정수표현으로 바꾸어 메모리 사용량을 줄이는 기법
- 온디바이스
- 자기 모델, 작은 모델 써서 금칙어 판정(가드레일) 같은거 하면 좋을것 같음 
- 사이즈(데이터타입)을 변환한다고 해서 새로운 모델을 다운받는것이 아닌 이미 존재하는 모델의 사이즈를 변환시키는것임 
-  Weight Quantization은 활성화 값도 조정 (weight only만 활성화 값 조정 없음)

#### activation quantization
- 뉴런이 입력을 처리한 중간 츨력값, 레이어를 통과할때 생성되는 텐서
- 입력 데이터에 따라 매번 달라지는 레이어의 중간 출력 텐서를 저비트의 정수 표현으로 변환하는 양자화 기법
- scale/zero(최댓값, 최솟값) 포인트를 찾아야 함, 즉 가중치와 크게 다르지 않음
  - 미리 대표값을 찾아서 미리 학습을 시켜 미리 값을 넣어서 한다 (추론시에는 아무 일도 안함)
    - 다만 이럴경우 예측성이 떨어지면 정확도가 떨어짐
  - 대표데이터를 넣지 말고 실시간으로 scale과 zero point 를 찾기
    - 미쳤니? ㅈㄴ 빡세 오버헤드 발생할수도 있음

#### kv cache quantization
- q,k,v 에서의 그 k,v 맞음
- 자기회귀 때문에 Q는 계속 바뀌기 때문에 k,v 만 캐싱함
  - Q는 계산할때 k,v 의 값이 필요함 즉 재활용이 가능하다는것
- 메모이제이션
- 계산양이 주는 대신 메모리 사용량이 늘게 됨
- 그러나 지금의 ai 추론이 여기에 너무 많은 의존을 하고 있음
- 이 k/v 벡터를 저비트로 저장하여 차지하는 용량을 줄이는 기법
- 성능에 영향이야 가겠지만 뭐...^^ 어쩌겠니

#### weight-only 양자화
- 가중치 온리
- 가중치와 다를게 없음

#### W8A8/W4A16
- W8A8 : 가중치도 8, 활성화도 8
- W4A16 : 가중치는 4, 활성화는 16

#### PTQ(post-training quantization)
- 학습이 완료된 모델을 재학습하지 않고 가중치나 활성화값의 수치 표현을 저비트로 표현하는 기법
- 즉 여태까지 배워온 방법들이 다 이 과정임
- 그 모델을 바탕으로 튜닝을 하는것
- 학습된 모델을 빠르게 사용하고 배포할 수 있음

#### 정적 양자화/ 동적 양자화
- 정적 양자화 : 대표 데이터를 미리 통과시켜 양자화 기준을 계산해두고 추론시에 그 값을 고정해서 사용하는 양자화 방식
  - =고정치 양자화와 비슷
- 동적 양자화 : 추론 시점에 들어오는 입력 데이터를 보고 활성화 값의 scale-zero point를 결정하는 방식
- 양자화 기준을 언제로 잡느냐에 따라 다름
- 장단점도 위 가중치 양자화와 비슷하것죠

#### calibration

#### QAT
- 학습하는 과정에 대해서 변경하는건 아니었음
- 모델 이름에 QAT 가 붙어있는 경우도 있음
- 추론시 적용될 양자화 오차를 미리 시뮬레이션 하여 저비트 추론 환경에서도 정확도를 유지하도록 적응시키는 학습 기법
- 가짜 양자화 노드를 사용하여 학습 시키는것이 핵심 (실제로 INT 연산자를 수행하지 않음, 흉내내기만 하는것)
  - 오차가 생기는것을 방지하기 위해 이렇게 행동하는것

#### outlier problem
- 이상치 : 같은 분포 내 일반 값 대비 수십~수백 배 큰 절댓닶을 가지는 극단값 (패턴을 벗어나는 값)
- 이상치 문제 : 소수의 극단값이 양자화의 표현 해상도를 잠식해 저비트 정확도가 급락하는 현상
- 일반적으로 이상치 제거를 통해 해결하지만 이상치가 중요한 값일 경우 문제가 생김
- 모델에 관심이 잇는 사람들은 남의 모델을 분석하는것도 좋아보임

#### GPTQ
- 우리가 아는 그 gpt 맞음 (not openAI, yes transformer mode)
- 단순 반올림보다 출력 품질이 덜 떨어지도록 오차를 보정하는 방법
- 한 가중치를 양자화하며 생긴 오차를 버리지 않고 아직 양자화 하지 않은 다른 가중치에 분배함
- hessian(헤시안) : 야코비안 -> 헤세 행렬
  - ai는 입력도 다변수이지만 출력도 다변수이다(스칼라를 모으면 다변수가되...)
  - 다변수함수의 이계도함수를 행렬로 표현한것

#### AWQ
- PTQ 기법, 출력에 중요한 가중치 채널을 찾아 저비트 양자화에서 덜 손상되도록 스케일을 조정하는 기법
- 무작위성이 핵심이 아닌 분포가 핵심이다


### 한줄정리
- llm optimization : 학습이 끝난 메모리 성능을 줄이는 등의 방식을 통하여 llm의 성능을 올리는 과정
#### 데이터 타입
- 데이터타입은 IEEE754를 따르며 지수에 어느정도 할당할것인지 가수에 어느정도 할당할것인지에 따라 크기와 타입이 달라짐 가령 FP16은 16비트중 1비트를 기호 5비트를 지수에 사용하며 가수는 10비트를 할당하는 방식
#### 양자화 기법
- 보통 fp32로 저장되어 있는 모델의 데이터 타입을 더 작은 단위로 변경함으로써 경량화 시키는 방식을 말함, 모델의 과정중에 어디에 도입되느냐에 따라 방식이 달라짐
- affine quantization : 평행 이동하는 기법
- weight quantization : 가중치에서 양자화를 하는 기법
- activation quantization : 활성함수에 양자화를 하는 기법
- kv cache quantization : 모델을 생성할때 자기회귀 추론시 사용되는 key갑과 value 값을 저장하여 재사용하는 기법을 말함, query의 경우 계속해서 변화하므로 재사용이 불사함
- weight only는 가중치에서만 W8A8, W4A16은 가중치와 활성함수에 어떤 데이터타입을 사용하여 양자화할지 정하여 적어둔 약어이다 (ex. W8A8 은 가중치 INT8 Activation INT8)
- PTQ : 
- 정적/동적 양자화 : 가중치 양자화에서의 고정양자화와 동적 양자화와 비슷한 개념으로 정적 양자화는 대표 데이터를 뽑아 미리 추론을 돌려 양자화값을 정해놓는 방식으로 정확도가 낮아질 우려가 있고 동덕 양자화는 실제로 입력값에 따라 추론값을 변경시켜 양자화를 정하는 방식을 말함
- calibration : 
- QAT : 
- outlier problem : 이상치는 일반적으로 패턴을 벗어나는 근사치가 없는 동떨어진 값을 의미하며 보통 제거하는 방식으로 처리하는게 일반적이나 이상치가 중요한 값일 경우 문제가 발생함
#### 고급 양자화 기법
- gptq : 단순 반올림 하는 방식보다 품질이 덜 떨어지도록 오차를 보정하는 방법으로 가중치를 양자화하며 생긴 오차값을 버리지 않고 아직 양자화 하지 않은 다른 가중치에 분배하여 사용함
- awq : 활성화 통계를 보고 출력에 중요한 가중치 채널을 찾아 저비트 양자화에서 덜 손상되도록 최대값(스케일)을 조정하는 ptq 기법으로 무작위성 보다는 분포를 보고 어떤 가중치를 더 조심스럽게 양자화 해야하는지 판단함


#### 오늘 후기
- 내일부터는 노트북이 좀 쪼개질덧, 교재에 합본 쪼갠본 다 올릴테니 참고하세욤
- 수학이 어려우면 ai 쌤 호출
- 